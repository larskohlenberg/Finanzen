# M2-Schulden-Abbau Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fünf bewusst aufgeschobene M2-Schulden abbauen: Hash/Click-Kollision bei Gegenbuchungen (#2), `cents()`-Robustheit (#4), hardcodierte deutsche Strings (#6), nicht-funktionale Checks-Tiles (#7) und das Transfer-Tile mit echtem Inhalt füllen (#8).

**Architecture:** Reine Frontend-Änderungen an der statischen Review-App (`app/`). Die App ist browser-only (klassische `<script>`-Dateien, kein Bundler, keine Node-Unit-Tests). Verifikation daher wie in M3 Task 9: `node --check <datei>` für Syntax plus Claude-Preview (`preview_eval`/`preview_snapshot`) für Verhalten gegen die laufende App auf `http://localhost:8765`. Logik-Helfer (`cents`, `transferChecks`) sind Top-Level-Funktionen im klassischen Script und damit im Browser-Scope global erreichbar — Verhalten wird per `preview_eval` gegen den echten geladenen Code geprüft.

**Tech Stack:** Vanilla JS (ES-Module-frei, klassische Scripts), `app/main.js` / `app/i18n.js` / `app/styles.css`, Server via `.claude/launch.json` (`python3 -m http.server 8765 --directory app`).

**Nicht in diesem Plan:** #1 (Full-Re-render — Handoff schiebt bewusst nach M4/M5/M9), #3 (`pushState` ohne URL-Update — bewusst akzeptiert), #5 (SVG-Icons — eigener Durchgang).

---

## File Structure

| Datei | Verantwortung in diesem Plan |
| --- | --- |
| `app/main.js` | Transfer-Zelle ohne Hash-Anker, `hashchange`-Listener entfernt, `cents()` robust, `accountTypeLabel` i18n, `aria-label` i18n, Checks-Tiles als `<div>`, `transferChecks()` + Transfer-Sektion |
| `app/i18n.js` | Neue Keys: `chrome.mainNav`, `accountTypes.{giro,tagesgeld,depot}`, `checksPage.{transfersLead,transferOk,transferIncomplete}` (DE + EN) |
| `app/styles.css` | `.tile-static` (Cursor/Hover neutralisieren) |

**Verifikations-Setup (einmalig vor Task 1, Server bleibt über alle Tasks laufen):**

```bash
# launch.json existiert bereits aus M3. Falls nicht, anlegen (siehe M3 Task 9 Step 7).
```

Server über das Preview-Tool starten (`preview_start` mit `name: "finanz-app"`), `serverId` merken. Nach jeder UI-Änderung im Browser `preview_eval("location.reload()")` bzw. erneutes `preview_snapshot`. Am Planende Server stoppen.

---

## Task 1: #2 Hash/Click-Kollision bei Gegenbuchung beseitigen

**Problem:** `app/main.js:465` rendert die Transfer-Zelle mit `data-action="paired-transfer"` am `<td>` UND einem inneren `<a href="#transaction=…" data-action="paired-transfer">`. Ein Klick löst den Click-Handler (`handleAction` → `commitNavigation` → `pushState`) und zusätzlich den `hashchange`-Listener (`app/main.js:819-833`, `replaceState`) aus → Doppel-Render und History-Risiko. Beide navigieren redundant zum selben Ziel.

**Entscheidung (Nutzer):** Anker + `hashchange`-Listener entfernen, den `data-action`-Click-Handler behalten.

**Files:**
- Modify: `app/main.js` (Zeile 465: inneres `<a>` → `<span>`; Zeilen 819-833: `hashchange`-Listener entfernen)

- [ ] **Step 1: Inneren Hash-Anker durch span ersetzen**

In `app/main.js`, Zeile 465, die `return`-Zeile von `renderTransferCell` (die Funktion, die mit `if (!paired) return ...` beginnt) ersetzen:

Alt:
```js
  return `<td class="transfer-link-cell" data-action="paired-transfer" data-transaction="${escapeHtml(paired.transaktion_id)}" title="${escapeHtml(t("transactions.pairedTransfer"))}"><a class="chip neutral linkish transfer-anchor" href="#transaction=${escapeHtml(paired.transaktion_id)}" data-action="paired-transfer" data-transaction="${escapeHtml(paired.transaktion_id)}">↔ ${escapeHtml(t("labels.yes"))}</a></td>`;
```

Neu:
```js
  return `<td class="transfer-link-cell" data-action="paired-transfer" data-transaction="${escapeHtml(paired.transaktion_id)}" title="${escapeHtml(t("transactions.pairedTransfer"))}"><span class="chip neutral linkish transfer-anchor">↔ ${escapeHtml(t("labels.yes"))}</span></td>`;
```

- [ ] **Step 2: `hashchange`-Listener entfernen**

In `app/main.js` den gesamten Block (Zeilen 819-833) löschen:

```js
window.addEventListener("hashchange", () => {
  const transactionId = location.hash.startsWith("#transaction=")
    ? decodeURIComponent(location.hash.replace("#transaction=", ""))
    : "";
  const transaction = transaktionenById.get(transactionId);
  if (!transaction) return;
  state.view = "transactions";
  state.selectedTransactionId = transactionId;
  state.transactionFilters.account = transaction.konto_id;
  state.transactionFilters.status = "";
  state.transactionFilters.category = "";
  state.transactionFilters.transfer = "";
  history.replaceState(snapshotState(), "", location.href);
  render();
});
```

Ersatzlos streichen. Der `popstate`-Listener und der `paired-transfer`-Zweig in `handleAction` (Zeile 755) bleiben unverändert und übernehmen die Navigation allein.

- [ ] **Step 3: Syntax prüfen**

Run: `node --check app/main.js`
Expected: kein Output, Exit 0.

- [ ] **Step 4: Verhalten im Browser prüfen**

Server neu laden (`preview_eval` mit `serverId`):
```js
(() => {
  location.reload();
  return "reloaded";
})()
```
Danach Transfer-Klick simulieren und prüfen, dass kein Hash gesetzt wird und die Auswahl korrekt umschaltet:
```js
(() => {
  // zur Transaktionsliste navigieren und eine Transfer-Zelle finden
  const cell = document.querySelector('.transfer-link-cell');
  if (!cell) return { error: "keine Transfer-Zelle sichtbar — erst zu Transaktionen navigieren" };
  const targetId = cell.dataset.transaction;
  cell.click();
  return {
    hash: location.hash,                       // erwartet: ""
    selected: window.state?.selectedTransactionId, // erwartet: targetId
    targetId,
    noAnchor: !document.querySelector('.transfer-anchor[href]'), // erwartet: true
  };
})()
```
Expected: `hash === ""`, `selected === targetId`, `noAnchor === true`.

- [ ] **Step 5: Commit**

```bash
git add app/main.js
git commit -m "fix(app): Hash/Click-Kollision bei Gegenbuchung beseitigen (M2-Schuld #2)"
```

---

## Task 2: #6 Hardcodierte deutsche Strings i18n-fähig machen

**Problem:** `accountTypeLabel` (`app/main.js:83-85`) gibt feste deutsche/capitalized Strings zurück; `aria-label="Hauptnavigation"` (`app/main.js:156`) ist hartcodiert. Im EN-Modus erscheinen sie weiterhin deutsch. Kontotyp-Werte im Datenstand: `giro`, `tagesgeld`, `depot`.

**Files:**
- Modify: `app/i18n.js` (neue Keys `chrome.mainNav` und Block `accountTypes` in DE + EN)
- Modify: `app/main.js` (`accountTypeLabel` nutzt i18n; `aria-label` nutzt `t`)

- [ ] **Step 1: i18n-Keys ergänzen (Deutsch)**

In `app/i18n.js` im deutschen `chrome`-Block (nach `importErrors: "Importfehler",`) ergänzen:
```js
      mainNav: "Hauptnavigation",
```

Im deutschen Sprachobjekt einen neuen `accountTypes`-Block direkt vor `status: {` (Zeile ~113) einfügen:
```js
    accountTypes: {
      giro: "Giro",
      tagesgeld: "Tagesgeld",
      depot: "Depot",
    },
```

- [ ] **Step 2: i18n-Keys ergänzen (Englisch)**

In `app/i18n.js` im englischen `chrome`-Block (nach `importErrors: "Import errors",`) ergänzen:
```js
      mainNav: "Main navigation",
```

Im englischen Sprachobjekt einen `accountTypes`-Block an der analogen Stelle (vor dem englischen `status: {`) einfügen:
```js
    accountTypes: {
      giro: "Checking",
      tagesgeld: "Savings",
      depot: "Depot",
    },
```

- [ ] **Step 3: `accountTypeLabel` auf i18n umstellen**

In `app/main.js` Zeilen 83-85 ersetzen:

Alt:
```js
function accountTypeLabel(type) {
  return type === "depot" ? "Depot" : type.charAt(0).toUpperCase() + type.slice(1);
}
```

Neu:
```js
function accountTypeLabel(type) {
  return t(`accountTypes.${type}`) || (type.charAt(0).toUpperCase() + type.slice(1));
}
```

(Fallback auf Capitalize, falls je ein unbekannter Typ auftaucht — `t()` liefert bei fehlendem Key den Key-String zurück; daher `||` mit Capitalize nur greifen lassen, wenn `t` leer/falsy ist. Da `t` den Key zurückgibt, ist der Fallback praktisch inaktiv, aber harmlos. Wenn `t` stattdessen `undefined` bei fehlendem Key liefert, greift der Capitalize-Fallback.)

- [ ] **Step 4: `aria-label` der Navigation i18n-fähig machen**

In `app/main.js` Zeile 156:

Alt:
```js
      <nav class="nav" aria-label="Hauptnavigation">
```

Neu:
```js
      <nav class="nav" aria-label="${escapeHtml(t("chrome.mainNav"))}">
```

- [ ] **Step 5: Syntax prüfen**

Run: `node --check app/main.js && node --check app/i18n.js`
Expected: kein Output, Exit 0.

- [ ] **Step 6: Verhalten im Browser prüfen (DE + EN)**

```js
(() => {
  const out = {};
  // Deutsch
  window.state.lang = "de"; render();
  out.de_nav = document.querySelector('nav.nav')?.getAttribute('aria-label');
  out.de_typ = (window.i18n?.de ? null : null); // Anzeige indirekt prüfen:
  window.state.lang = "en"; render();
  out.en_nav = document.querySelector('nav.nav')?.getAttribute('aria-label');
  // accountTypeLabel direkt prüfen
  out.giro_de = (window.state.lang = "de", render(), window.t ? window.t("accountTypes.giro") : "n/a");
  out.giro_en = (window.state.lang = "en", render(), window.t ? window.t("accountTypes.giro") : "n/a");
  window.state.lang = "de"; render();
  return out;
})()
```
Expected: `de_nav === "Hauptnavigation"`, `en_nav === "Main navigation"`, `giro_de === "Giro"`, `giro_en === "Checking"`.

> Hinweis: Falls `t` / `state` / `render` nicht global sichtbar sind (je nach Scope), stattdessen `preview_snapshot` im EN-Modus prüfen, dass in der Stammdaten-Kontotabelle „Checking/Savings/Depot" statt „Giro/Tagesgeld/Depot" steht. Zum EN-Wechsel im UI das Sprach-Dropdown nutzen (`preview_fill`/`preview_click`).

- [ ] **Step 7: Commit**

```bash
git add app/main.js app/i18n.js
git commit -m "fix(app): Kontotyp-Labels und Nav-aria-label i18n-fähig (M2-Schuld #6)"
```

---

## Task 3: #4 `cents()` robust gegen Stringformate machen

**Problem:** `cents()` (`app/main.js:62-66`) parst Strings manuell per `split(".")`. Funktioniert für das Schema-Format, ist aber fragil. Robuster: `Math.round(Number(s) * 100)`.

**Files:**
- Modify: `app/main.js` (`cents`, Zeilen 62-66)

- [ ] **Step 1: Implementierung ersetzen**

In `app/main.js` Zeilen 62-66:

Alt:
```js
function cents(decimalString) {
  const [euros, centsPart] = decimalString.replace("-", "").split(".");
  const sign = decimalString.startsWith("-") ? -1 : 1;
  return sign * (Number(euros) * 100 + Number(centsPart || 0));
}
```

Neu:
```js
function cents(decimalString) {
  return Math.round(Number(decimalString) * 100);
}
```

- [ ] **Step 2: Syntax prüfen**

Run: `node --check app/main.js`
Expected: kein Output, Exit 0.

- [ ] **Step 3: Verhalten im Browser prüfen (Äquivalenz auf bekannten Werten)**

```js
(() => {
  location.reload();
  return "reloaded";
})()
```
Dann (nach kurzem Moment) prüfen, dass `cents` die erwarteten Cent-Integer liefert:
```js
(() => {
  if (typeof cents !== "function") return { error: "cents nicht im globalen Scope — Äquivalenz stattdessen über Salden-Anzeige prüfen" };
  return {
    a: cents("3500.00"),  // 350000
    b: cents("-82.45"),   // -8245
    c: cents("0.00"),     // 0
    d: cents("0.05"),     // 5
    e: cents("-0.01"),    // -1
  };
})()
```
Expected: `{a:350000, b:-8245, c:0, d:5, e:-1}`.

> Falls `cents` nicht global: per `preview_snapshot` prüfen, dass die Salden-Summen in Übersicht und Transaktions-Filter unverändert/korrekt angezeigt werden (visuelle Äquivalenz).

- [ ] **Step 4: Commit**

```bash
git add app/main.js
git commit -m "fix(app): cents() robust via Math.round(Number*100) (M2-Schuld #4)"
```

---

## Task 4: #7 Checks-Tiles als nicht-interaktive Anzeige

**Problem:** Die Tiles in `renderChecks` (`app/main.js:580-586`) sind `<button class="tile">` mit `cursor: pointer` und Hover-/Focus-Highlight (`app/styles.css:688-701`), haben aber kein `data-action` → sie sehen klickbar aus, tun aber nichts.

**Entscheidung (Nutzer):** Als nicht-interaktive Anzeige (`<div>`) rendern.

**Files:**
- Modify: `app/main.js` (`renderChecks`, Tile-Markup)
- Modify: `app/styles.css` (neue Regel `.tile-static`)

- [ ] **Step 1: Tile-Markup von `<button>` auf `<div>` umstellen**

In `app/main.js` in `renderChecks` den Tile-Block ersetzen:

Alt:
```js
      ${groups.map(([label, checks]) => `
        <button class="tile">
          <strong>${escapeHtml(label)}</strong>
          <div class="count">${checks.length}</div>
          <span class="chip ${checks.some((check) => check.severity === "review") ? "review" : "success"}">${checks.some((check) => check.severity === "review") ? "?" : "✓"} ${escapeHtml(checks.length ? t("status.review") : t("status.success"))}</span>
        </button>
      `).join("")}
```

Neu:
```js
      ${groups.map(([label, checks]) => `
        <div class="tile tile-static">
          <strong>${escapeHtml(label)}</strong>
          <div class="count">${checks.length}</div>
          <span class="chip ${checks.some((check) => check.severity === "review") ? "review" : "success"}">${checks.some((check) => check.severity === "review") ? "?" : "✓"} ${escapeHtml(checks.length ? t("status.review") : t("status.success"))}</span>
        </div>
      `).join("")}
```

- [ ] **Step 2: CSS-Regel `.tile-static` ergänzen**

In `app/styles.css` direkt nach dem `.tile.active`-Block (nach Zeile 706) einfügen:

```css
.tile-static {
  cursor: default;
}

.tile-static:hover,
.tile-static:focus-visible {
  border-color: var(--border);
  outline: none;
}
```

- [ ] **Step 3: Syntax prüfen**

Run: `node --check app/main.js`
Expected: kein Output, Exit 0.

- [ ] **Step 4: Verhalten im Browser prüfen**

```js
(() => {
  location.reload();
  return "reloaded";
})()
```
Dann zur Checks-Ansicht navigieren und prüfen:
```js
(() => {
  window.state.view = "checks"; render();
  const tiles = [...document.querySelectorAll('.tile')];
  return {
    count: tiles.length,                                   // erwartet: 4
    allDiv: tiles.every(el => el.tagName === "DIV"),       // erwartet: true
    allStatic: tiles.every(el => el.classList.contains("tile-static")), // erwartet: true
    cursor: tiles[0] ? getComputedStyle(tiles[0]).cursor : null,        // erwartet: "default"
  };
})()
```
Expected: `count === 4`, `allDiv === true`, `allStatic === true`, `cursor === "default"`.

- [ ] **Step 5: Commit**

```bash
git add app/main.js app/styles.css
git commit -m "fix(app): Checks-Tiles als nicht-interaktive Anzeige (M2-Schuld #7)"
```

---

## Task 5: #8 Transfer-Tile mit echter Transfer-Prüfung füllen

**Problem:** Die Transfers-Gruppe in `renderChecks` ist hartcodiert leer (`[t("checksPage.transfers"), []]`), das Tile zeigt darum fix `0` und einen grünen Erfolgs-Chip, obwohl keine Prüfung existiert. M3 liefert jetzt echte Transfers; das Review-Bundle enthält `data.transfers` (`app/review-data.js:153`).

**Entscheidung (Nutzer):** Echte Transfer-Prüfung anzeigen.

**Prüflogik:** Pro Transfer in `data.transfers` ist die Paarung „vollständig", wenn beide referenzierten Transaktionen existieren, beide `ist_transfer === true` sind und beider `transfer_id` zur Transfer-ID passt. Sonst „unvollständig" (Review).

**Files:**
- Modify: `app/main.js` (`transferChecks()`-Helfer, Transfers-Gruppe in `renderChecks`, Transfer-Sektion)
- Modify: `app/i18n.js` (neue Keys `checksPage.transfersLead`, `checksPage.transferOk`, `checksPage.transferIncomplete`, DE + EN)

- [ ] **Step 1: i18n-Keys ergänzen (DE + EN)**

In `app/i18n.js` im deutschen `checksPage`-Block (nach `importErrorsLead: ...`) ergänzen:
```js
      transfersLead: "Automatisch gepaarte Transfers aus dem Import. Externe Transfers markiert der Nutzer manuell.",
      transferOk: "Vollständig gepaart",
      transferIncomplete: "Unvollständig",
```

Im englischen `checksPage`-Block analog (nach `importErrorsLead: ...`):
```js
      transfersLead: "Auto-paired transfers from import. External transfers are marked manually by the user.",
      transferOk: "Fully paired",
      transferIncomplete: "Incomplete",
```

- [ ] **Step 2: `transferChecks()`-Helfer ergänzen**

In `app/main.js` direkt vor `function renderChecks()` (Zeile 570) einfügen:

```js
function transferChecks() {
  return (data.transfers || []).map((transfer) => {
    const abgang = transaktionenById.get(transfer.abgang_transaktion_id);
    const zugang = transaktionenById.get(transfer.zugang_transaktion_id);
    const ok =
      !!abgang &&
      !!zugang &&
      abgang.ist_transfer === true &&
      zugang.ist_transfer === true &&
      abgang.transfer_id === transfer.transfer_id &&
      zugang.transfer_id === transfer.transfer_id;
    return { transfer_id: transfer.transfer_id, betrag: transfer.betrag, ok };
  });
}
```

- [ ] **Step 3: Transfers-Gruppe in `renderChecks` mit echten Werten füllen**

In `app/main.js` in `renderChecks` die `groups`-Definition anpassen. Vor `const groups = [` eine Zeile einfügen und die Transfers-Gruppe ersetzen:

Alt:
```js
  const groups = [
    [t("checksPage.validation"), data.checks.filter((check) => check.scope === "datenstand")],
    [t("checksPage.categories"), data.checks.filter((check) => check.scope === "transaktion")],
    [t("checksPage.accountReferences"), data.checks.filter((check) => check.scope === "konto")],
    [t("checksPage.transfers"), []],
  ];
```

Neu:
```js
  const transferResults = transferChecks();
  const groups = [
    [t("checksPage.validation"), data.checks.filter((check) => check.scope === "datenstand")],
    [t("checksPage.categories"), data.checks.filter((check) => check.scope === "transaktion")],
    [t("checksPage.accountReferences"), data.checks.filter((check) => check.scope === "konto")],
    [t("checksPage.transfers"), transferResults.map((tc) => ({ severity: tc.ok ? "success" : "review" }))],
  ];
```

- [ ] **Step 4: Transfer-Sektion in `renderChecks` rendern**

In `app/main.js` in `renderChecks` nach der bestehenden Checks-`<section>` und (falls vorhanden) vor/neben der Importfehler-Sektion eine Transfer-Sektion einfügen. Konkret direkt vor dem schließenden Backtick des Template-Strings (vor der Importfehler-Sektion aus M3, falls die zuerst kommt — Reihenfolge egal) einfügen:

```js
    ${transferResults.length > 0 ? `
      <section class="panel panel-pad" style="margin-top: 16px;">
        <h2 class="section-title">${escapeHtml(t("checksPage.transfers"))}</h2>
        <p class="page-lead">${escapeHtml(t("checksPage.transfersLead"))}</p>
        <div class="rail-list">
          ${transferResults.map((tc) => `
            <div class="rail-item">
              <span class="chip ${tc.ok ? "success" : "review"}">${tc.ok ? "✓" : "?"} ${escapeHtml(tc.ok ? t("checksPage.transferOk") : t("checksPage.transferIncomplete"))}</span>
              <span>${escapeHtml(tc.transfer_id)} · ${escapeHtml(formatMoney(cents(tc.betrag)))}</span>
            </div>
          `).join("")}
        </div>
      </section>
    ` : ""}
```

- [ ] **Step 5: Syntax prüfen**

Run: `node --check app/main.js && node --check app/i18n.js`
Expected: kein Output, Exit 0.

- [ ] **Step 6: Verhalten im Browser prüfen**

```js
(() => {
  location.reload();
  return "reloaded";
})()
```
Dann zur Checks-Ansicht navigieren und prüfen, dass das Transfer-Tile die echte Anzahl zeigt und die Sektion den Transfer listet:
```js
(() => {
  window.state.view = "checks"; render();
  const tiles = [...document.querySelectorAll('.tile')];
  const transferTile = tiles.find(el => /Transfers/i.test(el.querySelector('strong')?.textContent || ""));
  const section = [...document.querySelectorAll('.section-title')].find(h => /Transfers/i.test(h.textContent));
  const railItem = section?.closest('section')?.querySelector('.rail-item')?.textContent?.replace(/\s+/g,' ').trim();
  return {
    tileCount: transferTile?.querySelector('.count')?.textContent,   // erwartet: "1"
    tileChip: transferTile?.querySelector('.chip')?.textContent?.trim(), // ✓ vollständig → success
    railItem,  // erwartet: enthält "TRF-20260505-001" und Betrag
  };
})()
```
Expected: `tileCount === "1"`; `railItem` enthält `TRF-20260505-001` und einen formatierten Betrag (z. B. `500,00 €`).

- [ ] **Step 7: Screenshot zur Sichtprüfung**

`preview_screenshot` mit `serverId`. Erwartung: Transfer-Tile zeigt `1` mit grünem Erfolgs-Chip (Demo-Transfer ist vollständig gepaart); darunter eine „Transfers"-Sektion mit einem Eintrag „✓ Vollständig gepaart · TRF-20260505-001 · 500,00 €".

- [ ] **Step 8: Commit**

```bash
git add app/main.js app/i18n.js
git commit -m "fix(app): Transfer-Tile mit echter Transfer-Prüfung füllen (M2-Schuld #8)"
```

---

## Abschluss

- [ ] **Server stoppen** (`preview_stop` mit `serverId`).
- [ ] **Gesamtcheck:** `node --check app/main.js && node --check app/i18n.js && node --check app/review-data.js && npm test && npm run validate:m1` — App-Syntax grün, 39 M3-Tests weiterhin grün, `M1 validation passed` (die Änderungen berühren keine Tool-/Datenlogik).
- [ ] **Handoff aktualisieren:** In `docs/handoff/Handoff_Runde2.md` im Abschnitt „Bewusste M2-Schulden" vermerken, welche Punkte erledigt sind (#2, #4, #6, #7, #8) und dass #1/#3 bewusst offen bleiben, #5 einen eigenen Durchgang bekommt. Commit: `docs: M2-Schulden #2/#4/#6/#7/#8 als erledigt vermerkt`.
- [ ] **Branch abschließen** via `superpowers:finishing-a-development-branch`.

---

## Self-Review-Notizen (vom Plan-Autor)

- **Spec-Abdeckung:** #2 (Task 1), #6 (Task 2), #4 (Task 3), #7 (Task 4), #8 (Task 5). #1/#3/#5 explizit ausgeschlossen mit Begründung. Vollständig.
- **Verifikationsmodell:** App ist browser-only; daher `node --check` + Preview-`eval`/`snapshot` statt `node --test` (konsistent mit M3 Task 9). Für Logik-Helfer (`cents`, `transferChecks`) wird das echte geladene Verhalten per `preview_eval` geprüft; Fallback auf visuelle Äquivalenz dokumentiert, falls Funktionen nicht im globalen Scope liegen.
- **Typkonsistenz:** `transferChecks()` liefert `{ transfer_id, betrag, ok }`; in `groups` auf `{ severity }` gemappt (Tile nutzt `checks.some(c => c.severity === "review")` und `checks.length`); in der Sektion direkt verwendet. Konsistent über Steps 2-4 von Task 5.
- **Risiko `cents`/`t`-Scope:** Falls `t`, `state`, `render`, `cents` nicht als globale Symbole im Browser sichtbar sind, greifen die dokumentierten visuellen Fallbacks (Snapshot/Screenshot). Kein Step hängt zwingend an globaler Sichtbarkeit.
- **Bewusste Auslassung:** Keine neuen Daten/Schemas; reine UI. `npm test`/`validate:m1` dienen nur als Regressions-Sicherung, nicht als Abnahme der UI-Fixes.

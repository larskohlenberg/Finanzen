# M7 Vorsorge — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Versicherungen und Renten als **eine** `vorsorge`-Entität (ADR 0021) erfassen — mit Rückkaufswert (Aktivum), erwarteten Leistungen (Anwartschaft) und Beitrags-Verknüpfung — so dass Arbeitsende- und Todesfall-Szenarien (M6) ehrlich rechnen.

**Architecture:** Eine neue Stammdaten-Entität `vorsorge` (Datei `app/data/{master,demo}/vorsorge.json`), validiert über `validate-core.mjs`. Werte leben als **Zeitwerte** (`feld ∈ {rueckkaufswert, erwartete_rente, erwartete_kapitalleistung}`, `entitaet = "vorsorge"`). Die reine Mathematik kommt in die bestehenden Engines: `vermoegen.mjs` nimmt **kapitalbildende** Verträge ins Nettovermögen, `szenarien.mjs` friert den Rückkaufswert als Sachwert ein und löst eine neue Annahme-Art `vorsorge-leistung` zur Rechenzeit in vorhandene Primitive (`regelzahlung-neu` / `einmalzahlung` + Gegenbuchung `ziel_typ: vorsorge`) auf. Drei neue Checks (`vorsorge-ungeprueft`, `vorsorge-wiedervorlage`, `vorsorge-wechsel`). Eine neue View `app/views/vorsorge.mjs`. Komplexe, vielschichtige Demo-Daten (Familie Schneider).

**Tech Stack:** Vanilla ES-Module (`.mjs`/`.js`), `node:test`, keine Build-Tools, keine Fremd-Libs. Cent-Integer-Arithmetik. SVG via `app/charts.mjs`.

## Global Constraints

- **Geldbeträge:** auf Platte Decimal-String `^-?\d+\.\d{2}$`; intern Cent-Integer; Konvertierung nur über `toCents`. Verbatim ADR 0004.
- **Datumswerte:** ISO `YYYY-MM-DD`, keine Zeit/Zeitzone.
- **IDs:** Stammdaten-Konvention `^VS-\d{3}$` für Vorsorge (sequenziell, wie `KAT-\d{3}`).
- **Reine Funktionen:** `vermoegen.mjs` und `szenarien.mjs` importieren **keine** Node-I/O (browser-ladbar).
- **App ist nur Anzeige (ADR 0006):** keine Schreib-/CRUD-Funktion in der View; Vorsorge entsteht über den Agenten.
- **Datenqualität sitzt am Wert, nicht an der Entität** (CONTEXT.md): die `vorsorge`-Entität trägt **keine** `qualitaet`; Qualität lebt am Zeitwert.
- **Achsen der Entität:** `status` (Lebenszyklus) + `geprueft_am` (Review + Aktualität). Kein `geprueft`-Bool.
- **Tests:** `node --test tests/*.test.mjs`; nach jeder Task `npm test` grün + `npm run validate:master` grün.
- **Engine rechnet live ab heute:** Rechenstichtag = `today` (Parameter, nicht `stand`).
- **Verweise:** CONTEXT.md (`## Vorsorge`), ADR 0021, `docs/runde2/Meilensteine_Runde2.md > M7`, `docs/handoffs/handoff-m7-vorsorge.md` (historische Vorlage — diese Plan-Entscheidungen haben Vorrang).

---

### Task 1: `vorsorge.schema.json` + Validator-Collection

Die neue Entität als JSON-Schema (Doku/Vertrag) und als deklarative Collection im Validator. Realer Master-Bestand ist `[]` (Datei wird in Task 12 als Demo befüllt), daher bricht nichts.

**Files:**
- Create: `app/schemas/vorsorge.schema.json`
- Modify: `app/tools/validate-core.mjs` (im `schemas`-Objekt, nach `vermoegenswerte`, vor `kategorisierungsregeln`)
- Test: `tests/m7-vorsorge.test.mjs` (neu)

**Interfaces:**
- Produces: Collection `vorsorge` (optional). Felder: `vorsorge_id ^VS-\d{3}$`, `art ∈ {lebensversicherung, rentenversicherung, gesetzliche-rente, betriebsrente, riester, ruerup, schutzversicherung, sonstig}`, `name` (minLength 1), `person_id ^PER-\d{3}$`, `status ∈ {aktiv, gekuendigt, ruhend, geplant, laufend, beendet}`, `kapitalbildend` (boolean), `kapitalwahl ∈ {offen, monatsrente, kapital}` (optional), `geprueft_am` (date, optional), `leistung_beginn` (date, optional), `ersetzt_vorsorge_id ^VS-\d{3}$` (optional), `aktiv_bis` (date, optional), `quelle_hinweis`/`quelle_standdatum`/`bemerkung` (optional). Required: `vorsorge_id`, `art`, `name`, `person_id`, `status`, `kapitalbildend`.

- [ ] **Step 1: Failing test schreiben**

Neue Datei `tests/m7-vorsorge.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { validateMasterData } from "../app/tools/validate-core.mjs";

function basis(extra = {}) {
  return {
    personen: [{ person_id: "PER-001", name: "Lena", status: "aktiv" }],
    ...extra,
  };
}

test("valide Vorsorge (kapitalbildende Riester) besteht", () => {
  const data = basis({
    vorsorge: [{
      vorsorge_id: "VS-001", art: "riester", name: "Riester Lena", person_id: "PER-001",
      status: "aktiv", kapitalbildend: true, kapitalwahl: "offen", geprueft_am: "2026-01-15",
    }],
  });
  assert.deepEqual(validateMasterData(data).errors, []);
});

test("unbekannte art ist Fehler", () => {
  const data = basis({ vorsorge: [{ vorsorge_id: "VS-001", art: "krypto", name: "X", person_id: "PER-001", status: "aktiv", kapitalbildend: false }] });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("art")));
});

test("fehlendes kapitalbildend ist Fehler", () => {
  const data = basis({ vorsorge: [{ vorsorge_id: "VS-001", art: "schutzversicherung", name: "KFZ-HV", person_id: "PER-001", status: "aktiv" }] });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("kapitalbildend")));
});

test("kapitalbildend muss boolean sein", () => {
  const data = basis({ vorsorge: [{ vorsorge_id: "VS-001", art: "riester", name: "X", person_id: "PER-001", status: "aktiv", kapitalbildend: "ja" }] });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("kapitalbildend")));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge.test.mjs`
Expected: FAIL — `vorsorge` ist eine unbekannte Collection, der valide Fall meldet `vorsorge: unbekanntes Feld` o. ä. bzw. die Fehler-Asserts greifen nicht.

- [ ] **Step 3: JSON-Schema anlegen**

`app/schemas/vorsorge.schema.json`:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Vorsorge",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["vorsorge_id", "art", "name", "person_id", "status", "kapitalbildend"],
    "additionalProperties": false,
    "properties": {
      "vorsorge_id": { "type": "string", "pattern": "^VS-\\d{3}$" },
      "art": { "type": "string", "enum": ["lebensversicherung", "rentenversicherung", "gesetzliche-rente", "betriebsrente", "riester", "ruerup", "schutzversicherung", "sonstig"] },
      "name": { "type": "string", "minLength": 1 },
      "person_id": { "type": "string", "pattern": "^PER-\\d{3}$" },
      "status": { "type": "string", "enum": ["aktiv", "gekuendigt", "ruhend", "geplant", "laufend", "beendet"] },
      "kapitalbildend": { "type": "boolean" },
      "kapitalwahl": { "type": "string", "enum": ["offen", "monatsrente", "kapital"] },
      "geprueft_am": { "type": "string", "format": "date" },
      "leistung_beginn": { "type": "string", "format": "date" },
      "ersetzt_vorsorge_id": { "type": "string", "pattern": "^VS-\\d{3}$" },
      "aktiv_bis": { "type": "string", "format": "date" },
      "quelle_hinweis": { "type": "string" },
      "quelle_standdatum": { "type": "string", "format": "date" },
      "bemerkung": { "type": "string" }
    }
  }
}
```

- [ ] **Step 4: Validator-Collection ergänzen**

In `app/tools/validate-core.mjs`, im `schemas`-Objekt **nach** dem `vermoegenswerte`-Block einfügen:

```js
  vorsorge: {
    optional: true,
    required: ["vorsorge_id", "art", "name", "person_id", "status", "kapitalbildend"],
    fields: {
      vorsorge_id: { type: "string", pattern: /^VS-\d{3}$/ },
      art: { type: "string", enum: ["lebensversicherung", "rentenversicherung", "gesetzliche-rente", "betriebsrente", "riester", "ruerup", "schutzversicherung", "sonstig"] },
      name: { type: "string", minLength: 1 },
      person_id: { type: "string", pattern: /^PER-\d{3}$/ },
      status: { type: "string", enum: ["aktiv", "gekuendigt", "ruhend", "geplant", "laufend", "beendet"] },
      kapitalbildend: { type: "boolean" },
      kapitalwahl: { type: "string", enum: ["offen", "monatsrente", "kapital"] },
      geprueft_am: { type: "string", format: "date" },
      leistung_beginn: { type: "string", format: "date" },
      ersetzt_vorsorge_id: { type: "string", pattern: /^VS-\d{3}$/ },
      aktiv_bis: { type: "string", format: "date" },
      quelle_hinweis: { type: "string" },
      quelle_standdatum: { type: "string", format: "date" },
      bemerkung: { type: "string" },
    },
  },
```

- [ ] **Step 5: Tests grün**

Run: `node --test tests/m7-vorsorge.test.mjs` → PASS.
Run: `npm test` → alle grün.

- [ ] **Step 6: Commit**

```bash
git add app/schemas/vorsorge.schema.json app/tools/validate-core.mjs tests/m7-vorsorge.test.mjs
git commit -m "feat(m7): vorsorge-Schema und Validator-Collection"
```

---

### Task 2: Zeitwerte für Vorsorge (entitaet + drei felder)

Die drei nicht-vermischbaren Werte (`rueckkaufswert`, `erwartete_rente`, `erwartete_kapitalleistung`) leben als Zeitwerte mit `entitaet = "vorsorge"`. Schema-Enums erweitern, Referenzprüfung andocken.

**Files:**
- Modify: `app/schemas/zeitwerte.schema.json`
- Modify: `app/tools/validate-core.mjs` (Collection `zeitwerte` Enums + `zeitwertEntitaeten`-Map in `validateCrossFieldRules`)
- Test: `tests/m7-vorsorge.test.mjs`

**Interfaces:**
- Consumes: Collection `vorsorge` (Task 1).
- Produces: Zeitwert `entitaet ∈ {…, vorsorge}`, `feld ∈ {…, rueckkaufswert, erwartete_rente, erwartete_kapitalleistung}`; Cross-Field prüft `entitaet="vorsorge"` gegen existierende `vorsorge_id`.

- [ ] **Step 1: Failing test**

In `tests/m7-vorsorge.test.mjs` ergänzen:

```js
test("vorsorge-Zeitwert mit rueckkaufswert ist valide", () => {
  const data = basis({
    vorsorge: [{ vorsorge_id: "VS-001", art: "riester", name: "R", person_id: "PER-001", status: "aktiv", kapitalbildend: true }],
    zeitwerte: [{ entitaet: "vorsorge", entitaet_id: "VS-001", feld: "rueckkaufswert", wert: "9100.00", standdatum: "2026-01-01", qualitaet: "belegt" }],
  });
  assert.deepEqual(validateMasterData(data).errors, []);
});

test("vorsorge-Zeitwert auf unbekannte vorsorge_id ist Fehler", () => {
  const data = basis({
    vorsorge: [],
    zeitwerte: [{ entitaet: "vorsorge", entitaet_id: "VS-999", feld: "erwartete_rente", wert: "1480.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" }],
  });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("VS-999")));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge.test.mjs`
Expected: FAIL — `entitaet`/`feld` Enum kennt `vorsorge`/`rueckkaufswert` nicht (`Wert nicht erlaubt`).

- [ ] **Step 3: JSON-Schema erweitern**

In `app/schemas/zeitwerte.schema.json` die beiden Enums ergänzen (Werte anhängen): `entitaet` um `"vorsorge"`, `feld` um `"rueckkaufswert"`, `"erwartete_rente"`, `"erwartete_kapitalleistung"`.

- [ ] **Step 4: Validator-Enums + Referenzmap**

In `app/tools/validate-core.mjs`, Collection `zeitwerte`:

```js
      entitaet: { type: "string", enum: ["konto", "immobilie", "vermoegenswert", "darlehen", "vorsorge"] },
      entitaet_id: { type: "string", minLength: 1 },
      feld: { type: "string", enum: ["kontostand", "depotwert", "marktwert", "restschuld", "rueckkaufswert", "erwartete_rente", "erwartete_kapitalleistung"] },
```

In `validateCrossFieldRules`, die `zeitwertEntitaeten`-Map um Vorsorge erweitern (direkt nach `const vermoegenswerte = byId(...)` steht `const vorsorge = byId(data.vorsorge, "vorsorge_id");` — diese Zeile in Task 4 ergänzt; hier nur die Map):

```js
  const vorsorge = byId(data.vorsorge, "vorsorge_id");
  const zeitwertEntitaeten = {
    konto: konten, immobilie: immobilien, vermoegenswert: vermoegenswerte, darlehen, vorsorge,
  };
```

(Die vorhandene `zeitwertEntitaeten`-Definition ersetzen; `const vorsorge = …` einmalig einfügen.)

- [ ] **Step 5: Tests grün**

Run: `node --test tests/m7-vorsorge.test.mjs` → PASS.
Run: `npm test` → grün.

- [ ] **Step 6: Commit**

```bash
git add app/schemas/zeitwerte.schema.json app/tools/validate-core.mjs tests/m7-vorsorge.test.mjs
git commit -m "feat(m7): zeitwerte fuer vorsorge (rueckkaufswert/erwartete_rente/erwartete_kapitalleistung)"
```

---

### Task 3: Regelzahlung trägt `vorsorge_id` (Beitrags-Verknüpfung)

Einseitiger Link Beitrag→Vorsorge (CONTEXT, ADR 0021), symmetrisch zu `darlehen_id`. Optionales Feld + Referenzprüfung.

**Files:**
- Modify: `app/schemas/regelzahlungen.schema.json`
- Modify: `app/tools/validate-core.mjs` (Collection `regelzahlungen` + Cross-Field)
- Test: `tests/m7-vorsorge.test.mjs`

**Interfaces:**
- Produces: optionales Regelzahlung-Feld `vorsorge_id ^VS-\d{3}$`; Cross-Field meldet kaputte Referenz.

- [ ] **Step 1: Failing test**

```js
test("Regelzahlung mit gueltiger vorsorge_id ist valide", () => {
  const data = basis({
    vorsorge: [{ vorsorge_id: "VS-001", art: "schutzversicherung", name: "KFZ", person_id: "PER-001", status: "aktiv", kapitalbildend: false }],
    regelzahlungen: [{ regelzahlung_id: "RZ-001", bezeichnung: "KFZ-Beitrag", betrag: "-92.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2026-01-01", status: "bestaetigt", erstellt_am: "2026-01-01", vorsorge_id: "VS-001" }],
  });
  assert.deepEqual(validateMasterData(data).errors, []);
});

test("Regelzahlung mit unbekannter vorsorge_id ist Fehler", () => {
  const data = basis({
    vorsorge: [],
    regelzahlungen: [{ regelzahlung_id: "RZ-001", bezeichnung: "X", betrag: "-92.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2026-01-01", status: "bestaetigt", erstellt_am: "2026-01-01", vorsorge_id: "VS-404" }],
  });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("VS-404")));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge.test.mjs`
Expected: FAIL — `vorsorge_id: unbekanntes Feld` im ersten Test.

- [ ] **Step 3: Schema + Validator-Feld**

`app/schemas/regelzahlungen.schema.json`: in `properties` ergänzen `"vorsorge_id": { "type": "string", "pattern": "^VS-\\d{3}$" }`.

`app/tools/validate-core.mjs`, Collection `regelzahlungen`, `fields` ergänzen:

```js
      vorsorge_id: { type: "string", pattern: /^VS-\d{3}$/ },
```

- [ ] **Step 4: Cross-Field-Referenz**

In `validateCrossFieldRules`, den bestehenden Block `data.regelzahlungen?.forEach((rz) => { if (rz.darlehen_id …) })` (bei `darlehen.has`) um die Vorsorge-Referenz erweitern:

```js
  data.regelzahlungen?.forEach((rz) => {
    if (rz.darlehen_id && !darlehen.has(rz.darlehen_id)) {
      errors.push(`regelzahlungen.${rz.regelzahlung_id}.darlehen_id: ${rz.darlehen_id} existiert nicht`);
    }
    if (rz.vorsorge_id && !vorsorge.has(rz.vorsorge_id)) {
      errors.push(`regelzahlungen.${rz.regelzahlung_id}.vorsorge_id: ${rz.vorsorge_id} existiert nicht`);
    }
  });
```

(`const vorsorge = byId(data.vorsorge, "vorsorge_id");` aus Task 2 wird hier mitgenutzt.)

- [ ] **Step 5: Tests grün**

Run: `node --test tests/m7-vorsorge.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 6: Commit**

```bash
git add app/schemas/regelzahlungen.schema.json app/tools/validate-core.mjs tests/m7-vorsorge.test.mjs
git commit -m "feat(m7): regelzahlung.vorsorge_id (einseitige Beitrags-Verknuepfung)"
```

---

### Task 4: Vorsorge-Cross-Field (person_id, Nachfolge) + Annahme-Art `vorsorge-leistung` validieren

Referentielle Integrität der Vorsorge selbst: `person_id` muss existieren, `ersetzt_vorsorge_id` muss auf eine existierende, **andere** Vorsorge zeigen (in `validateCrossFieldRules`). Außerdem **muss** `validateSzenarien` die neue Annahme-Art `vorsorge-leistung` **akzeptieren** — die bestehende Whitelist (`einmalzahlung | regelzahlung-neu | regelzahlung-aenderung`) lehnt sie sonst mit „art unbekannt" ab und bricht die Demo-Validierung. Dort auch `arm`-Enum + `vorsorge_id`-Verweis + `ab`-Datum prüfen.

**Files:**
- Modify: `app/tools/validate-core.mjs` (`validateCrossFieldRules` **und** `validateSzenarien`)
- Test: `tests/m7-vorsorge.test.mjs`

**Interfaces:**
- Produces: Fehler bei kaputter `person_id`/`ersetzt_vorsorge_id`; `validateSzenarien` akzeptiert `art: "vorsorge-leistung"` und meldet unbekannte `vorsorge_id`, unbekanntes `arm`, fehlendes `ab`.

- [ ] **Step 1: Failing test**

```js
test("Vorsorge mit unbekannter person_id ist Fehler", () => {
  const data = basis({ vorsorge: [{ vorsorge_id: "VS-001", art: "riester", name: "R", person_id: "PER-999", status: "aktiv", kapitalbildend: true }] });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("PER-999")));
});

test("ersetzt_vorsorge_id muss auf existierende Vorsorge zeigen", () => {
  const data = basis({ vorsorge: [{ vorsorge_id: "VS-002", art: "schutzversicherung", name: "KFZ neu", person_id: "PER-001", status: "aktiv", kapitalbildend: false, ersetzt_vorsorge_id: "VS-001" }] });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("ersetzt_vorsorge_id")));
});

test("vorsorge-leistung-Annahme mit unbekannter vorsorge_id ist Fehler", () => {
  const data = basis({
    vorsorge: [],
    szenarien: [{ szenario_id: "SZN-001", name: "Ruhestand", status: "entwurf", stand: "2026-06-28", reichweite_bis: "2050-12-31", erstellt_am: "2026-06-28",
      annahmen: [{ annahme_id: "A1", art: "vorsorge-leistung", vorsorge_id: "VS-777", arm: "rente", ab: "2042-01-01" }] }],
  });
  assert.ok(validateMasterData(data).errors.some((e) => e.includes("VS-777")));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge.test.mjs`
Expected: FAIL — keine dieser Prüfungen existiert.

- [ ] **Step 3: Entity-Cross-Field-Block ergänzen**

In `validateCrossFieldRules`, **vor** `validateSzenarien(data, errors);`, einfügen:

```js
  data.vorsorge?.forEach((vs) => {
    if (!personen.has(vs.person_id)) {
      errors.push(`vorsorge.${vs.vorsorge_id}.person_id: ${vs.person_id} existiert nicht`);
    }
    if (vs.ersetzt_vorsorge_id) {
      if (vs.ersetzt_vorsorge_id === vs.vorsorge_id) {
        errors.push(`vorsorge.${vs.vorsorge_id}.ersetzt_vorsorge_id: darf nicht auf sich selbst zeigen`);
      } else if (!vorsorge.has(vs.ersetzt_vorsorge_id)) {
        errors.push(`vorsorge.${vs.vorsorge_id}.ersetzt_vorsorge_id: ${vs.ersetzt_vorsorge_id} existiert nicht`);
      }
    }
  });
```

- [ ] **Step 4: `validateSzenarien` um `vorsorge-leistung` erweitern**

In `app/tools/validate-core.mjs`, Funktion `validateSzenarien`:

(a) Bei den ID-Mengen (nach `const rzIds = …`) eine Vorsorge-Menge ergänzen:

```js
  const vorsorgeIds = new Set((data.vorsorge ?? []).map((v) => v.vorsorge_id));
```

(b) Die Art-Whitelist (aktuell `if (!["einmalzahlung", "regelzahlung-neu", "regelzahlung-aenderung"].includes(a.art))`) um `"vorsorge-leistung"` erweitern:

```js
      if (!["einmalzahlung", "regelzahlung-neu", "regelzahlung-aenderung", "vorsorge-leistung"].includes(a.art)) {
        errors.push(`${ap}: art unbekannt`);
        continue;
      }
```

(c) **Nach** dem `regelzahlung-aenderung`-Block (nach dessen `continue;`) einen eigenen `vorsorge-leistung`-Block einfügen (er endet ebenfalls mit `continue;`, überspringt also die `einmalzahlung`/`regelzahlung-neu`-Pflichtfelder):

```js
      if (a.art === "vorsorge-leistung") {
        if (!vorsorgeIds.has(a.vorsorge_id)) errors.push(`${ap}: vorsorge_id ${a.vorsorge_id} existiert nicht`);
        if (!["rente", "kapital"].includes(a.arm)) errors.push(`${ap}: arm ungueltig (rente|kapital)`);
        if (!isIsoDate(a.ab)) errors.push(`${ap}: ab fehlt/ungueltig`);
        if (a.gegenbuchung) errors.push(`${ap}: vorsorge-leistung darf keine gegenbuchung haben (wird zur Rechenzeit erzeugt)`);
        continue;
      }
```

(Die `qualitaet`-Prüfung bei Zeile 496 läuft davor und gilt auch hier — Demo-`vorsorge-leistung`-Annahmen tragen daher `qualitaet`.)

- [ ] **Step 5: Tests grün**

Run: `node --test tests/m7-vorsorge.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 6: Commit**

```bash
git add app/tools/validate-core.mjs tests/m7-vorsorge.test.mjs
git commit -m "feat(m7): vorsorge cross-field (person/nachfolge) + validateSzenarien vorsorge-leistung"
```

---

### Task 5: Nettovermögen — kapitalbildende Vorsorge-Positionen

`vermoegen.mjs` nimmt **nur** `kapitalbildend`-Verträge auf, Wert = jüngster `rueckkaufswert`-Zeitwert. Ohne Zeitwert → sichtbare Lücke (`fehlt: true`, Qualität `offen`). `!kapitalbildend` erzeugt **nie** eine Position. Vorsorge ist personengebunden, voller Wert in die Haushaltssicht (ADR 0014, keine Anteilsgewichtung).

**Files:**
- Modify: `app/vermoegen.mjs` (`computeNettovermoegen`, neuer Block nach `vermoegenswerte`)
- Test: `tests/m7-vorsorge-vermoegen.test.mjs` (neu)

**Interfaces:**
- Consumes: `aktuellerZeitwert(zeitwerte, "vorsorge", id, "rueckkaufswert")`.
- Produces: Positionen `{ klasse: "vorsorge", id, name, wert_cents, basis, qualitaet, standdatum, fehlt }`; nur für `kapitalbildend`.

- [ ] **Step 1: Failing test**

Neue Datei `tests/m7-vorsorge-vermoegen.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeNettovermoegen } from "../app/vermoegen.mjs";

const TODAY = "2026-06-28";

function data(vorsorge, zeitwerte = []) {
  return { konten: [], immobilien: [], vermoegenswerte: [], darlehen: [], transaktionen: [], vorsorge, zeitwerte };
}

test("kapitalbildende Vorsorge mit Rueckkaufswert zaehlt als Aktivum", () => {
  const r = computeNettovermoegen(data(
    [{ vorsorge_id: "VS-003", art: "riester", name: "Riester Lena", person_id: "PER-001", status: "aktiv", kapitalbildend: true }],
    [{ entitaet: "vorsorge", entitaet_id: "VS-003", feld: "rueckkaufswert", wert: "9100.00", standdatum: "2026-01-01", qualitaet: "belegt" }],
  ), TODAY);
  assert.equal(r.aktiva_cents, 910000);
  assert.ok(r.positionen.some((p) => p.klasse === "vorsorge" && p.wert_cents === 910000 && p.fehlt === false));
});

test("kapitalbildende Vorsorge ohne Zeitwert ist sichtbare Luecke", () => {
  const r = computeNettovermoegen(data(
    [{ vorsorge_id: "VS-003", art: "riester", name: "Riester", person_id: "PER-001", status: "aktiv", kapitalbildend: true }],
  ), TODAY);
  assert.equal(r.aktiva_cents, 0);
  const p = r.positionen.find((p) => p.klasse === "vorsorge");
  assert.equal(p.fehlt, true);
  assert.equal(r.qualitaet.gesamt, "offen");
});

test("gesetzliche Rente (!kapitalbildend) erzeugt NIE eine Position", () => {
  const r = computeNettovermoegen(data(
    [{ vorsorge_id: "VS-001", art: "gesetzliche-rente", name: "GRV Lena", person_id: "PER-001", status: "geplant", kapitalbildend: false }],
    [{ entitaet: "vorsorge", entitaet_id: "VS-001", feld: "erwartete_rente", wert: "1480.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" }],
  ), TODAY);
  assert.equal(r.positionen.filter((p) => p.klasse === "vorsorge").length, 0);
  assert.equal(r.aktiva_cents, 0);
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-vermoegen.test.mjs`
Expected: FAIL — keine Vorsorge-Positionen.

- [ ] **Step 3: Implementierung**

In `app/vermoegen.mjs`, `computeNettovermoegen`, **nach** der `vermoegenswerte`-Schleife (vor dem `darlehen`-Block) einfügen:

```js
  for (const vs of data.vorsorge ?? []) {
    if (!vs.kapitalbildend) continue;
    const rw = aktuellerZeitwert(data.zeitwerte, "vorsorge", vs.vorsorge_id, "rueckkaufswert");
    if (!rw) { fehlend++; positionen.push({ klasse: "vorsorge", id: vs.vorsorge_id, name: vs.name, wert_cents: 0, basis: "rueckkaufswert-fehlt", qualitaet: null, standdatum: null, fehlt: true }); continue; }
    const cents = toCents(rw.wert);
    aktiva += cents;
    if (rw.qualitaet === "belegt") belegt++; else if (rw.qualitaet === "geschaetzt") geschaetzt++;
    positionen.push({ klasse: "vorsorge", id: vs.vorsorge_id, name: vs.name, wert_cents: cents, basis: "rueckkaufswert", qualitaet: rw.qualitaet, standdatum: rw.standdatum, fehlt: false });
  }
```

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-vermoegen.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Commit**

```bash
git add app/vermoegen.mjs tests/m7-vorsorge-vermoegen.test.mjs
git commit -m "feat(m7): kapitalbildende vorsorge im nettovermoegen"
```

---

### Task 6: Checks `vorsorge-ungeprueft` + `vorsorge-wiedervorlage`

`computeVermoegenChecks` warnt bei ungeprüften Ansprüchen (`geprueft_am` fehlt) und bei veralteten Prüfungen/Werten (jüngstes relevantes Datum älter als 12 Monate). Schwelle als Konstante.

**Files:**
- Modify: `app/vermoegen.mjs` (`STANDDATUM_SCHWELLEN` + `computeVermoegenChecks`, neuer Block am Ende vor `return checks;`)
- Test: `tests/m7-vorsorge-checks.test.mjs` (neu)

**Interfaces:**
- Produces: Checks `{ art: "vorsorge-ungeprueft" | "vorsorge-wiedervorlage", entitaet: "vorsorge", entitaet_id, text }`.

- [ ] **Step 1: Failing test**

Neue Datei `tests/m7-vorsorge-checks.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { computeVermoegenChecks } from "../app/vermoegen.mjs";

const TODAY = "2026-06-28";
const base = { konten: [], immobilien: [], vermoegenswerte: [], darlehen: [], transaktionen: [], regelzahlungen: [] };

test("vorsorge-ungeprueft feuert ohne geprueft_am", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [{ vorsorge_id: "VS-006", art: "betriebsrente", name: "bAV Lena", person_id: "PER-001", status: "geplant", kapitalbildend: false }],
    zeitwerte: [{ entitaet: "vorsorge", entitaet_id: "VS-006", feld: "erwartete_rente", wert: "240.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" }],
  }, TODAY);
  assert.ok(checks.some((c) => c.art === "vorsorge-ungeprueft" && c.entitaet_id === "VS-006"));
});

test("vorsorge-ungeprueft verschwindet mit geprueft_am", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [{ vorsorge_id: "VS-006", art: "betriebsrente", name: "bAV", person_id: "PER-001", status: "geplant", kapitalbildend: false, geprueft_am: "2026-03-01" }],
    zeitwerte: [{ entitaet: "vorsorge", entitaet_id: "VS-006", feld: "erwartete_rente", wert: "240.00", standdatum: "2026-03-01", qualitaet: "geschaetzt" }],
  }, TODAY);
  assert.ok(!checks.some((c) => c.art === "vorsorge-ungeprueft"));
});

test("vorsorge-wiedervorlage feuert bei alter Pruefung", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [{ vorsorge_id: "VS-001", art: "gesetzliche-rente", name: "GRV", person_id: "PER-001", status: "geplant", kapitalbildend: false, geprueft_am: "2024-01-01" }],
    zeitwerte: [{ entitaet: "vorsorge", entitaet_id: "VS-001", feld: "erwartete_rente", wert: "1480.00", standdatum: "2024-01-01", qualitaet: "geschaetzt" }],
  }, TODAY);
  assert.ok(checks.some((c) => c.art === "vorsorge-wiedervorlage" && c.entitaet_id === "VS-001"));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-checks.test.mjs`
Expected: FAIL — Checks fehlen.

- [ ] **Step 3: Implementierung**

In `app/vermoegen.mjs` die Konstante `STANDDATUM_SCHWELLEN` um `vorsorge: 12` ergänzen. In `computeVermoegenChecks`, **vor** `return checks;`, einfügen:

```js
  for (const vs of data.vorsorge ?? []) {
    if (vs.status === "beendet" || vs.status === "gekuendigt") continue;
    if (!vs.geprueft_am) {
      checks.push({ art: "vorsorge-ungeprueft", entitaet: "vorsorge", entitaet_id: vs.vorsorge_id, text: `Vorsorge ${vs.name}: ungeprüfter Anspruch — darf nicht als sicherer Zukunftswert gelten` });
      continue;
    }
    const felder = ["rueckkaufswert", "erwartete_rente", "erwartete_kapitalleistung"];
    let juengstes = vs.geprueft_am;
    for (const feld of felder) {
      const zw = aktuellerZeitwert(data.zeitwerte, "vorsorge", vs.vorsorge_id, feld);
      if (zw && zw.standdatum > juengstes) juengstes = zw.standdatum;
    }
    if (monateZwischen(juengstes, today) >= STANDDATUM_SCHWELLEN.vorsorge) {
      checks.push({ art: "vorsorge-wiedervorlage", entitaet: "vorsorge", entitaet_id: vs.vorsorge_id, text: `Vorsorge ${vs.name}: letzte Prüfung/Mitteilung vom ${juengstes} älter als ${STANDDATUM_SCHWELLEN.vorsorge} Monate — Werte verifizieren (z. B. Gesetzesänderung)` });
    }
  }
```

(`monateZwischen` und `aktuellerZeitwert` sind im Modul bereits definiert.)

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-checks.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Commit**

```bash
git add app/vermoegen.mjs tests/m7-vorsorge-checks.test.mjs
git commit -m "feat(m7): checks vorsorge-ungeprueft + vorsorge-wiedervorlage"
```

---

### Task 7: Szenario — Rückkaufswert als eingefrorener Sachwert

`sachwertStartwerte` in `szenarien.mjs` nimmt **kapitalbildende** Vorsorge-Positionen auf (eingefrorener Rückkaufswert, Schlüssel `vorsorge:${id}`), damit sie in der Nettovermögens-Projektion erscheinen und (Task 8) abbaubar sind. Voller Wert (keine Anteilsgewichtung).

**Files:**
- Modify: `app/szenarien.mjs` (`sachwertStartwerte`)
- Test: `tests/m7-vorsorge-szenario.test.mjs` (neu)

**Interfaces:**
- Produces: in `sachwertStartwerte` zusätzliche Keys `vorsorge:${id}` mit Cent-Wert; Qualität fließt in `qualitaeten`.

- [ ] **Step 1: Failing test**

Neue Datei `tests/m7-vorsorge-szenario.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { rechneSzenario } from "../app/szenarien.mjs";

const TODAY = "2026-06-28";

function baseData(extra = {}) {
  return {
    konten: [{ konto_id: "KTO-001", name: "Giro", kontotyp: "giro", status: "aktiv", liquiditaetsrelevant: true }],
    immobilien: [], vermoegenswerte: [], darlehen: [], regelzahlungen: [], transaktionen: [],
    zeitwerte: [{ entitaet: "konto", entitaet_id: "KTO-001", feld: "kontostand", wert: "5000.00", standdatum: "2026-06-01", qualitaet: "belegt" }],
    vorsorge: [],
    ...extra,
  };
}

test("kapitalbildende Vorsorge erhoeht das Netto im Szenario (eingefroren)", () => {
  const data = baseData({
    vorsorge: [{ vorsorge_id: "VS-003", art: "riester", name: "Riester", person_id: "PER-001", status: "aktiv", kapitalbildend: true }],
  });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-003", feld: "rueckkaufswert", wert: "9100.00", standdatum: "2026-01-01", qualitaet: "belegt" });
  const szn = { szenario_id: "SZN-001", name: "Basis", status: "entwurf", stand: TODAY, reichweite_bis: "2028-12-31", erstellt_am: TODAY, annahmen: [] };
  const r = rechneSzenario(data, szn, TODAY);
  // erster Monatspunkt enthaelt den eingefrorenen Rueckkaufswert in sachwerte_cents
  assert.equal(r.punkte[0].sachwerte_cents, 910000);
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs`
Expected: FAIL — `sachwerte_cents` ist 0 (Vorsorge nicht erfasst).

- [ ] **Step 3: Implementierung**

In `app/szenarien.mjs`, Funktion `sachwertStartwerte`, **nach** der `vermoegenswerte`-Schleife (vor `return { werte, qualitaeten };`) einfügen:

```js
  for (const vs of data.vorsorge ?? []) {
    if (!vs.kapitalbildend) continue;
    if (vs.status === "beendet" || vs.status === "gekuendigt") continue;
    const zw = aktuellerZeitwert(data.zeitwerte, "vorsorge", vs.vorsorge_id, "rueckkaufswert", today);
    werte.set(`vorsorge:${vs.vorsorge_id}`, zw ? toCents(zw.wert) : 0);
    qualitaeten.push(zw ? zw.qualitaet : "offen");
  }
```

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Commit**

```bash
git add app/szenarien.mjs tests/m7-vorsorge-szenario.test.mjs
git commit -m "feat(m7): rueckkaufswert als eingefrorener sachwert im szenario"
```

---

### Task 8: Szenario — Gegenbuchung `ziel_typ: vorsorge` (Abbau)

`sammleSachwertGegenbuchungen` erkennt `ziel_typ: "vorsorge"` und baut die eingefrorene Position bei Auszahlung (Fälligkeit/Tod) ab — gleiche Mechanik wie Immobilie/Vermögenswert. So entsteht keine Doppelzählung (Aktivum + ausgezahltes Cash).

**Files:**
- Modify: `app/szenarien.mjs` (`sammleSachwertGegenbuchungen`)
- Test: `tests/m7-vorsorge-szenario.test.mjs`

**Interfaces:**
- Consumes: `gegenbuchung = { ziel_typ: "vorsorge", ziel_id }`; Startwerte aus Task 7 (`vorsorge:${id}`).
- Produces: bei `einmalzahlung` mit `gegenbuchung.ziel_typ="vorsorge"` ein Abbau-Ereignis (`abbau_key = vorsorge:${id}`).

- [ ] **Step 1: Failing test**

In `tests/m7-vorsorge-szenario.test.mjs` ergänzen:

```js
test("Kapitalauszahlung (gegenbuchung vorsorge) baut Rueckkaufswert ab, Cash steigt", () => {
  const data = baseData({
    vorsorge: [{ vorsorge_id: "VS-003", art: "lebensversicherung", name: "LV", person_id: "PER-001", status: "aktiv", kapitalbildend: true }],
  });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-003", feld: "rueckkaufswert", wert: "9100.00", standdatum: "2026-01-01", qualitaet: "belegt" });
  const szn = { szenario_id: "SZN-002", name: "Kapital", status: "entwurf", stand: TODAY, reichweite_bis: "2028-12-31", erstellt_am: TODAY,
    annahmen: [{ annahme_id: "A1", art: "einmalzahlung", datum: "2027-01-15", betrag: "9100.00", qualitaet: "belegt", gegenbuchung: { ziel_typ: "vorsorge", ziel_id: "VS-003" } }] };
  const r = rechneSzenario(data, szn, TODAY);
  const letzter = r.punkte[r.punkte.length - 1];
  // Umschichtung: Aktivum (sachwerte) weg, Cash entsprechend hoeher -> netto ~ unveraendert
  assert.equal(letzter.sachwerte_cents, 0);
  assert.equal(letzter.liquide_cents, 500000 + 910000); // Startsaldo 5000 + 9100 Kapital
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs`
Expected: FAIL — `ziel_typ "vorsorge"` wird ignoriert; `sachwerte_cents` bleibt 910000, Cash ohne Zufluss.

- [ ] **Step 3: Implementierung**

In `app/szenarien.mjs`, `sammleSachwertGegenbuchungen`, die Typprüfung erweitern:

```js
    if (!g || (g.ziel_typ !== "immobilie" && g.ziel_typ !== "vermoegenswert" && g.ziel_typ !== "vorsorge")) continue;
```

(Der restliche Abbau-/Aufbau-Code bleibt unverändert; `abbau_key = vorsorge:${g.ziel_id}` greift automatisch über `sachwertStart`.)

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Commit**

```bash
git add app/szenarien.mjs tests/m7-vorsorge-szenario.test.mjs
git commit -m "feat(m7): gegenbuchung ziel_typ vorsorge (rueckkaufswert-abbau bei auszahlung)"
```

---

### Task 9: Szenario — Annahme-Art `vorsorge-leistung`

Die neue Annahme referenziert eine Vorsorge + Arm (`rente`/`kapital`); die Engine löst den `erwartete_*`-Zeitwert **zur Rechenzeit** auf und expandiert in vorhandene Primitive: `rente` → unbefristete `regelzahlung-neu` (lebenslanges Einkommen) **plus** — bei kapitalbildenden Verträgen — eine 0-Cash-`einmalzahlung` mit Gegenbuchung (Verrentung baut den Rückkaufswert ab); `kapital` → `einmalzahlung` (Kapitalzufluss) mit Gegenbuchung bei kapitalbildenden Verträgen. Qualität: ungeprüft (`geprueft_am` fehlt) ⇒ `offen`, sonst Qualität des Zeitwerts.

**Files:**
- Modify: `app/szenarien.mjs` (neue Funktion `expandVorsorgeLeistungen`, Aufruf am Anfang von `rechneSzenario`)
- Test: `tests/m7-vorsorge-szenario.test.mjs`

**Interfaces:**
- Consumes: Annahme `{ art: "vorsorge-leistung", annahme_id, vorsorge_id, arm: "rente"|"kapital", ab, bis? }`; `aktuellerZeitwert(...,"vorsorge", id, feld, today)`.
- Produces: `expandVorsorgeLeistungen(data, szenario, today) -> { szenario, warnungen }` (Annahmen ersetzt durch `regelzahlung-neu`/`einmalzahlung`-Primitive). `rechneSzenario` arbeitet danach auf dem expandierten Szenario.

- [ ] **Step 1: Failing test**

In `tests/m7-vorsorge-szenario.test.mjs` ergänzen:

```js
test("vorsorge-leistung rente erzeugt lebenslanges Einkommen", () => {
  const data = baseData({
    vorsorge: [{ vorsorge_id: "VS-001", art: "gesetzliche-rente", name: "GRV Lena", person_id: "PER-001", status: "geplant", kapitalbildend: false, geprueft_am: "2026-01-01" }],
  });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-001", feld: "erwartete_rente", wert: "1480.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" });
  const szn = { szenario_id: "SZN-003", name: "Ruhestand", status: "entwurf", stand: TODAY, reichweite_bis: "2027-06-30", erstellt_am: TODAY,
    annahmen: [{ annahme_id: "A1", art: "vorsorge-leistung", vorsorge_id: "VS-001", arm: "rente", ab: "2026-07-01" }] };
  const r = rechneSzenario(data, szn, TODAY);
  // 12 Monate Rente bis Juni 2027 -> Liquiditaet steigt deutlich gegenueber Startsaldo
  const letzter = r.punkte[r.punkte.length - 1];
  assert.ok(letzter.liquide_cents > 500000 + 11 * 148000);
});

test("vorsorge-leistung kapital bei kapitalbildender LV: Zufluss + Rueckkaufswert-Abbau", () => {
  const data = baseData({
    vorsorge: [{ vorsorge_id: "VS-004", art: "rentenversicherung", name: "Privat-RV", person_id: "PER-002", status: "aktiv", kapitalbildend: true, kapitalwahl: "kapital", geprueft_am: "2026-01-01" }],
  });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-004", feld: "rueckkaufswert", wert: "23400.00", standdatum: "2026-01-01", qualitaet: "belegt" });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-004", feld: "erwartete_kapitalleistung", wert: "31000.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" });
  const szn = { szenario_id: "SZN-004", name: "Kapital", status: "entwurf", stand: TODAY, reichweite_bis: "2028-12-31", erstellt_am: TODAY,
    annahmen: [{ annahme_id: "A1", art: "vorsorge-leistung", vorsorge_id: "VS-004", arm: "kapital", ab: "2027-01-15" }] };
  const r = rechneSzenario(data, szn, TODAY);
  const letzter = r.punkte[r.punkte.length - 1];
  assert.equal(letzter.sachwerte_cents, 0);                 // Rueckkaufswert abgebaut
  assert.equal(letzter.liquide_cents, 500000 + 3100000);    // Kapitalleistung zugeflossen
});

test("ungepruefte Anwartschaft zieht Szenario-Qualitaet auf offen", () => {
  const data = baseData({
    vorsorge: [{ vorsorge_id: "VS-006", art: "betriebsrente", name: "bAV", person_id: "PER-001", status: "geplant", kapitalbildend: false }],
  });
  data.zeitwerte.push({ entitaet: "vorsorge", entitaet_id: "VS-006", feld: "erwartete_rente", wert: "240.00", standdatum: "2026-01-01", qualitaet: "geschaetzt" });
  const szn = { szenario_id: "SZN-005", name: "bAV", status: "entwurf", stand: TODAY, reichweite_bis: "2027-06-30", erstellt_am: TODAY,
    annahmen: [{ annahme_id: "A1", art: "vorsorge-leistung", vorsorge_id: "VS-006", arm: "rente", ab: "2026-07-01" }] };
  const r = rechneSzenario(data, szn, TODAY);
  assert.equal(r.qualitaet, "offen");
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs`
Expected: FAIL — `vorsorge-leistung` wird von keiner Engine-Stelle interpretiert.

- [ ] **Step 3: Implementierung**

In `app/szenarien.mjs`, **vor** `export function rechneSzenario`, einfügen:

```js
// vorsorge-leistung-Annahmen in vorhandene Primitive aufloesen: Arm 'rente' -> unbefristete
// regelzahlung-neu (lebenslanges Einkommen) + bei kapitalbildenden Vertraegen ein 0-Cash-
// einmalzahlung-Abbau des Rueckkaufswerts (Verrentung); Arm 'kapital' -> einmalzahlung
// (Kapitalzufluss) + Abbau bei kapitalbildend. Qualitaet: ungeprueft -> offen.
export function expandVorsorgeLeistungen(data, szenario, today) {
  const annahmen = [];
  const warnungen = [];
  for (const a of szenario.annahmen ?? []) {
    if (a.art !== "vorsorge-leistung") { annahmen.push(a); continue; }
    const vs = (data.vorsorge ?? []).find((v) => v.vorsorge_id === a.vorsorge_id);
    if (!vs) { warnungen.push({ code: "vorsorge-unbekannt", text: `Annahme ${a.annahme_id} verweist auf unbekannte Vorsorge ${a.vorsorge_id}` }); continue; }
    const feld = a.arm === "kapital" ? "erwartete_kapitalleistung" : "erwartete_rente";
    const zw = aktuellerZeitwert(data.zeitwerte, "vorsorge", a.vorsorge_id, feld, today);
    if (!zw) { warnungen.push({ code: "vorsorge-leistung-ohne-wert", text: `Vorsorge ${a.vorsorge_id} hat keinen ${feld}-Zeitwert` }); continue; }
    const qualitaet = vs.geprueft_am ? (zw.qualitaet ?? "offen") : "offen";
    if (a.arm === "kapital") {
      annahmen.push({ annahme_id: a.annahme_id, art: "einmalzahlung", datum: a.ab, betrag: zw.wert, qualitaet,
        gegenbuchung: vs.kapitalbildend ? { ziel_typ: "vorsorge", ziel_id: vs.vorsorge_id } : undefined });
    } else {
      annahmen.push({ annahme_id: a.annahme_id, art: "regelzahlung-neu", name: vs.name, betrag: zw.wert, qualitaet,
        rhythmus_einheit: "monat", rhythmus_intervall: 1, ab: a.ab, bis: a.bis });
      if (vs.kapitalbildend) {
        annahmen.push({ annahme_id: `${a.annahme_id}~abbau`, art: "einmalzahlung", datum: a.ab, betrag: "0.00", qualitaet,
          gegenbuchung: { ziel_typ: "vorsorge", ziel_id: vs.vorsorge_id } });
      }
    }
  }
  return { szenario: { ...szenario, annahmen }, warnungen };
}
```

Dann in `rechneSzenario`, die **ersten Zeilen** des Funktionskörpers ersetzen:

```js
export function rechneSzenario(data, szenario, today) {
  const horizon = szenario.reichweite_bis;
  const warnungen = [];
  const { szenario: szenarioExpandiert, warnungen: vw } = expandVorsorgeLeistungen(data, szenario, today);
  szenario = szenarioExpandiert;
  warnungen.push(...vw);
  const { rzs, warnungen: mw } = modifizierteRegelzahlungen(data, szenario);
  warnungen.push(...mw);
```

(Rest der Funktion unverändert — alle weiteren `szenario.annahmen`-Zugriffe lesen jetzt das expandierte Szenario. `horizon` wird **vor** dem Reassign aus dem Original gelesen; `reichweite_bis` ist identisch.)

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-szenario.test.mjs` → PASS. `npm test` → grün (Basis-Rechnung mit leeren Annahmen unverändert: `computeSzenario` ruft `rechneSzenario` mit `annahmen: []`, die Expansion ist dann ein No-op).

- [ ] **Step 5: Commit**

```bash
git add app/szenarien.mjs tests/m7-vorsorge-szenario.test.mjs
git commit -m "feat(m7): annahme-art vorsorge-leistung (live-aufloesung in primitive)"
```

---

### Task 10: Check `vorsorge-wechsel`

Master-Daten-Kontinuität: für jede Vorsorge mit Beitrags-Regelzahlung, deren `aktiv_bis` **in der Zukunft innerhalb eines Vorausschau-Fensters** liegt (`today < aktiv_bis <= today + 24 Monate`), prüfen, ob ein Nachfolger existiert (`ersetzt_vorsorge_id` zeigt zurück) und lückenlos anschließt. Vergangene Enden warnen nie.

**Files:**
- Modify: `app/vermoegen.mjs` (`computeVermoegenChecks`)
- Test: `tests/m7-vorsorge-checks.test.mjs`

**Interfaces:**
- Produces: Checks `{ art: "vorsorge-wechsel", entitaet: "vorsorge", entitaet_id, text }`.

- [ ] **Step 1: Failing test**

In `tests/m7-vorsorge-checks.test.mjs` ergänzen:

```js
test("vorsorge-wechsel warnt bei kuenftigem Beitragsende ohne Nachfolger", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [{ vorsorge_id: "VS-007", art: "schutzversicherung", name: "KFZ-HV alt", person_id: "PER-001", status: "gekuendigt", kapitalbildend: false, geprueft_am: "2026-01-01" }],
    regelzahlungen: [{ regelzahlung_id: "RZ-014", bezeichnung: "KFZ-HV alt", betrag: "-92.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2025-01-01", aktiv_bis: "2026-12-31", status: "bestaetigt", erstellt_am: "2025-01-01", vorsorge_id: "VS-007" }],
  }, TODAY);
  assert.ok(checks.some((c) => c.art === "vorsorge-wechsel" && c.entitaet_id === "VS-007"));
});

test("vorsorge-wechsel schweigt mit lueckenlosem Nachfolger", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [
      { vorsorge_id: "VS-007", art: "schutzversicherung", name: "KFZ-HV alt", person_id: "PER-001", status: "gekuendigt", kapitalbildend: false, geprueft_am: "2026-01-01" },
      { vorsorge_id: "VS-008", art: "schutzversicherung", name: "KFZ-HV neu", person_id: "PER-001", status: "aktiv", kapitalbildend: false, geprueft_am: "2026-01-01", ersetzt_vorsorge_id: "VS-007" },
    ],
    regelzahlungen: [
      { regelzahlung_id: "RZ-014", bezeichnung: "KFZ alt", betrag: "-92.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2025-01-01", aktiv_bis: "2026-12-31", status: "bestaetigt", erstellt_am: "2025-01-01", vorsorge_id: "VS-007" },
      { regelzahlung_id: "RZ-015", bezeichnung: "KFZ neu", betrag: "-88.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2027-01-01", status: "bestaetigt", erstellt_am: "2026-12-01", vorsorge_id: "VS-008" },
    ],
  }, TODAY);
  assert.ok(!checks.some((c) => c.art === "vorsorge-wechsel"));
});

test("vorsorge-wechsel schweigt bei vergangenem Ende", () => {
  const checks = computeVermoegenChecks({ ...base,
    vorsorge: [{ vorsorge_id: "VS-007", art: "schutzversicherung", name: "alt", person_id: "PER-001", status: "gekuendigt", kapitalbildend: false, geprueft_am: "2026-01-01" }],
    regelzahlungen: [{ regelzahlung_id: "RZ-014", bezeichnung: "alt", betrag: "-92.00", rhythmus_einheit: "jahr", rhythmus_intervall: 1, anker_datum: "2024-01-01", aktiv_bis: "2025-06-30", status: "bestaetigt", erstellt_am: "2024-01-01", vorsorge_id: "VS-007" }],
  }, TODAY);
  assert.ok(!checks.some((c) => c.art === "vorsorge-wechsel"));
});
```

- [ ] **Step 2: Test rot**

Run: `node --test tests/m7-vorsorge-checks.test.mjs`
Expected: FAIL — `vorsorge-wechsel` existiert nicht.

- [ ] **Step 3: Implementierung**

In `app/vermoegen.mjs`, `computeVermoegenChecks`, **vor** `return checks;` (nach dem Block aus Task 6), einfügen:

```js
  const wechselHorizont = addInterval(today, "monat", 24);
  for (const vs of data.vorsorge ?? []) {
    // spaetestes kuenftiges Beitragsende dieser Vorsorge im Vorausschaufenster
    let ende = null;
    for (const rz of data.regelzahlungen ?? []) {
      if (rz.vorsorge_id !== vs.vorsorge_id || !rz.aktiv_bis) continue;
      if (rz.aktiv_bis > today && rz.aktiv_bis <= wechselHorizont) {
        if (!ende || rz.aktiv_bis > ende) ende = rz.aktiv_bis;
      }
    }
    if (!ende) continue;
    const nachfolger = (data.vorsorge ?? []).find((v) => v.ersetzt_vorsorge_id === vs.vorsorge_id);
    if (!nachfolger) {
      checks.push({ art: "vorsorge-wechsel", entitaet: "vorsorge", entitaet_id: vs.vorsorge_id, text: `Vorsorge ${vs.name}: Beitrag endet ${ende}, keine Nachfolge erkennbar — Wechsel oder bewusste Kündigung?` });
      continue;
    }
    const nachBeitragStart = (data.regelzahlungen ?? [])
      .filter((rz) => rz.vorsorge_id === nachfolger.vorsorge_id)
      .map((rz) => rz.anker_datum)
      .sort()[0];
    if (!nachBeitragStart || nachBeitragStart > addInterval(ende, "tag", 1)) {
      checks.push({ art: "vorsorge-wechsel", entitaet: "vorsorge", entitaet_id: vs.vorsorge_id, text: `Vorsorge ${vs.name}: Deckungslücke — alt endet ${ende}, Nachfolge ${nachfolger.name} beginnt ${nachBeitragStart ?? "ohne Beitrag"}` });
    }
  }
```

(`addInterval` ist aus `liquiditaet.mjs` bereits in `vermoegen.mjs` importiert.)

- [ ] **Step 4: Tests grün**

Run: `node --test tests/m7-vorsorge-checks.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Commit**

```bash
git add app/vermoegen.mjs tests/m7-vorsorge-checks.test.mjs
git commit -m "feat(m7): check vorsorge-wechsel (kontinuitaet bei beitragsende)"
```

---

### Task 11: View `app/views/vorsorge.mjs` + Nav, Routing, i18n

Reine Anzeige-View (ADR 0006): Liste der Vorsorge-Einträge mit Art, Person, Status, `kapitalbildend`-Badge, aktuellem Rückkaufswert/erwarteter Leistung (aus Zeitwerten), `geprueft`/`ungeprüft`-Badge, verknüpftem Beitrag und einer nicht-wegklickbaren Hinweisbox „Ungeprüfte Ansprüche werden nicht als sichere Zukunftswerte gerechnet". Muster wie `app/views/vermoegen.mjs`.

**Files:**
- Create: `app/views/vorsorge.mjs`
- Modify: `app/routing.mjs` (Route `vorsorge` registrieren — Muster wie `vermoegen`)
- Modify: `app/main.js` (View einhängen + Nav-Eintrag, wie `vermoegen`)
- Modify: `app/i18n.js` (Keys `nav.vorsorge`, `vorsorge.title`, `vorsorge.lead`, `vorsorge.kapitalbildend`, `vorsorge.rueckkaufswert`, `vorsorge.erwarteteRente`, `vorsorge.erwarteteKapitalleistung`, `vorsorge.beitrag`, `vorsorge.geprueft`, `vorsorge.ungeprueft`, `vorsorge.ungeprueftHinweis`, `vorsorge.kapitalwahl`, `vorsorge.nachfolger` — DE Default, EN analog)
- Modify: `app/styles.css` (Badges `kapitalbildend`/`ungeprueft` — vorhandene Badge-Sprache wiederverwenden)
- Test: `tests/m7-vorsorge-view.test.mjs` (neu, reine Render-Funktion ohne DOM, wie bei bestehenden View-Tests)

**Interfaces:**
- Consumes: `computeNettovermoegen` (Vorsorge-Positionen), `aktuellerZeitwert`, `computeVermoegenChecks` (Vorsorge-Checks).
- Produces: `renderVorsorge(data, today) -> { title, html }` (Signatur an `app/views/vermoegen.mjs` angleichen — dort exakte Form prüfen).

- [ ] **Step 1: Bestehendes View-Muster lesen**

`app/views/vermoegen.mjs` und ein bestehender View-Test (`tests/` nach `view` greppen) als Vorlage öffnen. Exporte, Render-Signatur und i18n-Aufruf 1:1 übernehmen.

- [ ] **Step 2: Failing test**

In `tests/m7-vorsorge-view.test.mjs`: eine Vorsorge mit Rückkaufswert + eine ungeprüfte Anwartschaft rendern; asserten, dass die HTML-Ausgabe den Vertragsnamen, den formatierten Rückkaufswert und das `ungeprüft`-Badge enthält. (Konkrete Assert-Strings nach der in Step 1 gelesenen Render-Signatur formulieren.)

- [ ] **Step 3: Test rot**

Run: `node --test tests/m7-vorsorge-view.test.mjs` → FAIL (Datei/Funktion fehlt).

- [ ] **Step 4: View implementieren**

`renderVorsorge` analog `renderVermoegen`: Tabelle je Vorsorge-Eintrag; Spalten Name/Art/Person/Status; Wert-Spalte zeigt bei `kapitalbildend` den `rueckkaufswert`, sonst die `erwartete_rente`/`erwartete_kapitalleistung` mit Label „Anwartschaft (nicht im Vermögen)"; Badges `kapitalbildend`/`ungeprüft`; Beitrag aus `regelzahlungen.filter(rz => rz.vorsorge_id === vs.vorsorge_id)`; Hinweisbox immer sichtbar.

- [ ] **Step 5: Verdrahten**

Route, Nav-Eintrag, i18n-Keys (DE/EN), Badge-Styles ergänzen — jeweils dem `vermoegen`-Vorbild folgend.

- [ ] **Step 6: Tests grün + manuelle Sichtprüfung**

Run: `node --test tests/m7-vorsorge-view.test.mjs` → PASS. `npm test` → grün. App starten (`python3 serve_app.py`), Tab „Vorsorge" öffnen, Demo-Modus (nach Task 12) prüfen.

- [ ] **Step 7: Commit**

```bash
git add app/views/vorsorge.mjs app/routing.mjs app/main.js app/i18n.js app/styles.css tests/m7-vorsorge-view.test.mjs
git commit -m "feat(m7): vorsorge-view + nav/routing/i18n"
```

---

### Task 12: Demo-Daten — komplexes, vielschichtiges Vorsorge-Set (Familie Schneider)

Die Demo deckt **alle** Modellfälle ab: gesetzliche Renten (Anwartschaft), kapitalbildende Verträge mit Rückkaufswert-Historie und Kapitalwahlrecht, eine Risikolebensversicherung (Todesfallsumme, `!kapitalbildend`), eine ungeprüfte Betriebsrente (löst `vorsorge-ungeprueft` aus), einen KFZ-Versicherungswechsel mit lückenlosem Nachfolger, sowie zwei Demo-Szenarien (Ruhestand, Todesfall), die `vorsorge-leistung` und den Rückkaufswert-Abbau ausüben.

**Files:**
- Create: `app/data/demo/vorsorge.json`
- Modify: `app/data/demo/zeitwerte.jsonl` (Vorsorge-Zeitwerte mit Historie anhängen)
- Modify: `app/data/demo/regelzahlungen.json` (Beitrags-RZ mit `vorsorge_id` ergänzen)
- Modify: `app/data/demo/szenarien.json` (zwei Szenarien ergänzen)
- Create: `app/data/master/vorsorge.json` (leer `[]` — realer Bestand wird über den Agenten befüllt)
- Modify: `app/data-loader.mjs` (Datei `vorsorge.json` mit in den Lade-Contract aufnehmen — wie die übrigen Master-Dateien)
- Test: `tests/m7-vorsorge-demo.test.mjs` (neu)

**Interfaces:**
- Consumes: alle vorherigen Tasks.
- Produces: `app/data/demo/vorsorge.json` lädt + validiert; Demo-Szenarien rechnen ohne Fehler.

- [ ] **Step 1: Loader-Contract erweitern**

In `app/data-loader.mjs` `vorsorge.json` zur geladenen Dateiliste hinzufügen (gleiche Stelle wie `darlehen.json`/`vermoegenswerte.json`; Default leeres Array, falls Datei fehlt — Muster der vorhandenen optionalen Collections übernehmen).

- [ ] **Step 2: `app/data/master/vorsorge.json` anlegen**

```json
[]
```

- [ ] **Step 3: Demo `app/data/demo/vorsorge.json` schreiben**

```json
[
  { "vorsorge_id": "VS-001", "art": "gesetzliche-rente", "name": "Gesetzliche Rente Lena", "person_id": "PER-001", "status": "geplant", "kapitalbildend": false, "geprueft_am": "2026-01-20", "leistung_beginn": "2042-08-01", "quelle_hinweis": "Renteninformation 2026", "bemerkung": "Netto geschätzt nach KV/PV/Steuer; brutto 2.050 lt. Renteninfo 2026" },
  { "vorsorge_id": "VS-002", "art": "gesetzliche-rente", "name": "Gesetzliche Rente Martin", "person_id": "PER-002", "status": "geplant", "kapitalbildend": false, "geprueft_am": "2026-01-20", "leistung_beginn": "2045-05-01", "bemerkung": "Netto geschätzt; brutto 1.620 lt. Renteninfo 2026" },
  { "vorsorge_id": "VS-003", "art": "riester", "name": "Riester Lena (MusterversicherungA)", "person_id": "PER-001", "status": "aktiv", "kapitalbildend": true, "kapitalwahl": "offen", "geprueft_am": "2026-02-01", "leistung_beginn": "2042-08-01", "quelle_hinweis": "Standmitteilung 01/2026", "bemerkung": "Kapitalwahl offen: Verrentung oder Teilkapital" },
  { "vorsorge_id": "VS-004", "art": "rentenversicherung", "name": "Private RV Martin (Heidelberger)", "person_id": "PER-002", "status": "aktiv", "kapitalbildend": true, "kapitalwahl": "offen", "geprueft_am": "2026-02-01", "leistung_beginn": "2045-05-01", "quelle_hinweis": "Vertragsstand 2025" },
  { "vorsorge_id": "VS-005", "art": "lebensversicherung", "name": "Risiko-LV Martin", "person_id": "PER-002", "status": "aktiv", "kapitalbildend": false, "geprueft_am": "2026-02-01", "bemerkung": "Reine Risiko-LV — Todesfallsumme 200.000, kein Rückkaufswert" },
  { "vorsorge_id": "VS-006", "art": "betriebsrente", "name": "bAV Lena (Direktzusage)", "person_id": "PER-001", "status": "geplant", "kapitalbildend": false, "leistung_beginn": "2042-08-01", "bemerkung": "Ungeprüft — keine aktuelle Standmitteilung; löst vorsorge-ungeprueft aus" },
  { "vorsorge_id": "VS-007", "art": "schutzversicherung", "name": "KFZ-Haftpflicht (alt)", "person_id": "PER-002", "status": "gekuendigt", "kapitalbildend": false, "geprueft_am": "2026-03-01", "aktiv_bis": "2026-12-31", "bemerkung": "Zum Jahresende gewechselt" },
  { "vorsorge_id": "VS-008", "art": "schutzversicherung", "name": "KFZ-Haftpflicht (neu)", "person_id": "PER-002", "status": "aktiv", "kapitalbildend": false, "geprueft_am": "2026-12-01", "ersetzt_vorsorge_id": "VS-007", "bemerkung": "Nachfolger, lückenloser Anschluss ab 2027-01-01" },
  { "vorsorge_id": "VS-009", "art": "schutzversicherung", "name": "Hausratversicherung", "person_id": "PER-001", "status": "aktiv", "kapitalbildend": false, "geprueft_am": "2026-03-01" }
]
```

- [ ] **Step 4: Demo-Zeitwerte anhängen (`app/data/demo/zeitwerte.jsonl`)**

Folgende Zeilen ans Dateiende anhängen (Historie: zwei Standmitteilungen je kapitalbildendem Vertrag, mehrere Renteninfos):

```jsonl
{"entitaet":"vorsorge","entitaet_id":"VS-001","feld":"erwartete_rente","wert":"1380.00","standdatum":"2025-01-15","qualitaet":"geschaetzt","quelle_hinweis":"Renteninformation 2025 (netto geschätzt)"}
{"entitaet":"vorsorge","entitaet_id":"VS-001","feld":"erwartete_rente","wert":"1450.00","standdatum":"2026-01-20","qualitaet":"geschaetzt","quelle_hinweis":"Renteninformation 2026 (netto geschätzt)"}
{"entitaet":"vorsorge","entitaet_id":"VS-002","feld":"erwartete_rente","wert":"1180.00","standdatum":"2026-01-20","qualitaet":"geschaetzt","quelle_hinweis":"Renteninformation 2026 (netto geschätzt)"}
{"entitaet":"vorsorge","entitaet_id":"VS-003","feld":"rueckkaufswert","wert":"8200.00","standdatum":"2025-01-10","qualitaet":"belegt","quelle_hinweis":"Standmitteilung 01/2025"}
{"entitaet":"vorsorge","entitaet_id":"VS-003","feld":"rueckkaufswert","wert":"9100.00","standdatum":"2026-02-01","qualitaet":"belegt","quelle_hinweis":"Standmitteilung 01/2026"}
{"entitaet":"vorsorge","entitaet_id":"VS-003","feld":"erwartete_rente","wert":"115.00","standdatum":"2026-02-01","qualitaet":"geschaetzt","quelle_hinweis":"Standmitteilung 01/2026 (netto geschätzt)"}
{"entitaet":"vorsorge","entitaet_id":"VS-003","feld":"erwartete_kapitalleistung","wert":"24000.00","standdatum":"2026-02-01","qualitaet":"geschaetzt","quelle_hinweis":"Hochrechnung Standmitteilung 01/2026"}
{"entitaet":"vorsorge","entitaet_id":"VS-004","feld":"rueckkaufswert","wert":"21800.00","standdatum":"2025-02-01","qualitaet":"belegt","quelle_hinweis":"Vertragsstand 2024"}
{"entitaet":"vorsorge","entitaet_id":"VS-004","feld":"rueckkaufswert","wert":"23400.00","standdatum":"2026-02-01","qualitaet":"belegt","quelle_hinweis":"Vertragsstand 2025"}
{"entitaet":"vorsorge","entitaet_id":"VS-004","feld":"erwartete_rente","wert":"95.00","standdatum":"2026-02-01","qualitaet":"geschaetzt","quelle_hinweis":"Vertragsstand 2025 (netto geschätzt)"}
{"entitaet":"vorsorge","entitaet_id":"VS-004","feld":"erwartete_kapitalleistung","wert":"31000.00","standdatum":"2026-02-01","qualitaet":"geschaetzt","quelle_hinweis":"Vertragsstand 2025"}
{"entitaet":"vorsorge","entitaet_id":"VS-005","feld":"erwartete_kapitalleistung","wert":"200000.00","standdatum":"2026-02-01","qualitaet":"belegt","quelle_hinweis":"Police Risiko-LV (Todesfallsumme)"}
{"entitaet":"vorsorge","entitaet_id":"VS-006","feld":"erwartete_rente","wert":"240.00","standdatum":"2023-06-01","qualitaet":"geschaetzt","quelle_hinweis":"Alte Hochrechnung — ungeprüft"}
```

- [ ] **Step 5: Beitrags-Regelzahlungen mit `vorsorge_id` (`app/data/demo/regelzahlungen.json`)**

Folgende Einträge in das Array ergänzen (Riester-Beitrag, private-RV-Beitrag, Risiko-LV-Beitrag, KFZ alt/neu, Hausrat):

```json
{ "regelzahlung_id": "RZ-014", "bezeichnung": "Riester-Beitrag Lena", "betrag": "-162.00", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "anker_datum": "2026-01-01", "status": "bestaetigt", "erstellt_am": "2026-01-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-003" },
{ "regelzahlung_id": "RZ-015", "bezeichnung": "Private RV Martin", "betrag": "-150.00", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "anker_datum": "2026-01-01", "status": "bestaetigt", "erstellt_am": "2026-01-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-004" },
{ "regelzahlung_id": "RZ-016", "bezeichnung": "Risiko-LV Martin", "betrag": "-28.00", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "anker_datum": "2026-01-01", "status": "bestaetigt", "erstellt_am": "2026-01-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-005" },
{ "regelzahlung_id": "RZ-017", "bezeichnung": "KFZ-Haftpflicht (alt)", "betrag": "-92.00", "rhythmus_einheit": "jahr", "rhythmus_intervall": 1, "anker_datum": "2025-01-01", "aktiv_bis": "2026-12-31", "status": "bestaetigt", "erstellt_am": "2025-01-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-007" },
{ "regelzahlung_id": "RZ-018", "bezeichnung": "KFZ-Haftpflicht (neu)", "betrag": "-88.00", "rhythmus_einheit": "jahr", "rhythmus_intervall": 1, "anker_datum": "2027-01-01", "status": "bestaetigt", "erstellt_am": "2026-12-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-008" },
{ "regelzahlung_id": "RZ-019", "bezeichnung": "Hausratversicherung", "betrag": "-96.00", "rhythmus_einheit": "jahr", "rhythmus_intervall": 1, "anker_datum": "2026-04-01", "status": "bestaetigt", "erstellt_am": "2026-01-01", "kategorie_id": "KAT-007", "qualitaet": "belegt", "vorsorge_id": "VS-009" }
```

- [ ] **Step 6: Demo-Szenarien ergänzen (`app/data/demo/szenarien.json`)**

Zwei Szenarien ans Array anhängen:

```json
{
  "szenario_id": "SZN-004", "name": "Ruhestand Lena ab 2042", "status": "entwurf",
  "stand": "2026-06-22", "reichweite_bis": "2048-12-31", "erstellt_am": "2026-06-22",
  "beschreibung": "Lena geht 2042 in Rente: Gehalt endet, gesetzliche Rente + Riester (Verrentung) beginnen.",
  "annahmen": [
    { "annahme_id": "A1", "art": "regelzahlung-aenderung", "regelzahlung_id": "RZ-001", "aktion": "beenden", "ab": "2042-08-01", "qualitaet": "geschaetzt", "begruendung": "Renteneintritt Lena 2042" },
    { "annahme_id": "A2", "art": "vorsorge-leistung", "vorsorge_id": "VS-001", "arm": "rente", "ab": "2042-08-01", "qualitaet": "geschaetzt", "begruendung": "Gesetzliche Rente Lena, netto aus Renteninfo 2026" },
    { "annahme_id": "A3", "art": "vorsorge-leistung", "vorsorge_id": "VS-003", "arm": "rente", "ab": "2042-08-01", "qualitaet": "geschaetzt", "begruendung": "Riester-Verrentung; Rückkaufswert wird zu Rentenbeginn abgebaut" }
  ]
},
{
  "szenario_id": "SZN-005", "name": "Todesfall Martin 2028", "status": "entwurf",
  "stand": "2026-06-22", "reichweite_bis": "2040-12-31", "erstellt_am": "2026-06-22",
  "beschreibung": "Hypothese: Martin verstirbt 2028. Sein Gehalt endet, Risiko-LV zahlt Kapital, Lena erhält Witwenrente, die Kinder Waisenrente.",
  "annahmen": [
    { "annahme_id": "A1", "art": "regelzahlung-aenderung", "regelzahlung_id": "RZ-002", "aktion": "beenden", "ab": "2028-03-01", "qualitaet": "geschaetzt", "begruendung": "Wegfall Erwerbseinkommen Martin" },
    { "annahme_id": "A2", "art": "vorsorge-leistung", "vorsorge_id": "VS-005", "arm": "kapital", "ab": "2028-03-15", "qualitaet": "belegt", "begruendung": "Todesfallleistung Risiko-LV (200.000) an die Hinterbliebenen" },
    { "annahme_id": "A3", "art": "regelzahlung-neu", "name": "Witwenrente Lena", "betrag": "640.00", "qualitaet": "geschaetzt", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "ab": "2028-04-01", "begruendung": "Große Witwenrente ~55% der Anwartschaft Martin, netto geschätzt" },
    { "annahme_id": "A4", "art": "regelzahlung-neu", "name": "Waisenrente Emma", "betrag": "230.00", "qualitaet": "geschaetzt", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "ab": "2028-04-01", "bis": "2035-07-31", "begruendung": "Halbwaisenrente bis 18 (bzw. 27 in Ausbildung)" },
    { "annahme_id": "A5", "art": "regelzahlung-neu", "name": "Waisenrente Noah", "betrag": "230.00", "qualitaet": "geschaetzt", "rhythmus_einheit": "monat", "rhythmus_intervall": 1, "ab": "2028-04-01", "bis": "2037-09-30", "begruendung": "Halbwaisenrente bis 18" }
  ]
}
```

- [ ] **Step 7: Validierungs-/Rechen-Test**

Neue Datei `tests/m7-vorsorge-demo.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { validateMasterData } from "../app/tools/validate-core.mjs";
import { computeSzenario } from "../app/szenarien.mjs";

function ladeDemo() {
  const j = (p) => JSON.parse(readFileSync(new URL(`../app/data/demo/${p}`, import.meta.url)));
  const jsonl = (p) => readFileSync(new URL(`../app/data/demo/${p}`, import.meta.url), "utf8").trim().split("\n").map((l) => JSON.parse(l));
  return {
    personen: j("personen.json"), konten: j("konten.json"), kategorien: j("kategorien.json"),
    immobilien: j("immobilien.json"), darlehen: j("darlehen.json"), vermoegenswerte: j("vermoegenswerte.json"),
    regelzahlungen: j("regelzahlungen.json"), kategorisierungsregeln: j("kategorisierungsregeln.json"),
    transfers: j("transfers.json"), szenarien: j("szenarien.json"), vorsorge: j("vorsorge.json"),
    transaktionen: jsonl("transaktionen.jsonl"), zeitwerte: jsonl("zeitwerte.jsonl"),
  };
}

test("Demo-Daten inkl. Vorsorge sind valide", () => {
  assert.deepEqual(validateMasterData(ladeDemo()).errors, []);
});

test("Ruhestands-Szenario SZN-004 rechnet, Rente erhoeht Liquiditaet ggue. Basis", () => {
  const data = ladeDemo();
  const szn = data.szenarien.find((s) => s.szenario_id === "SZN-004");
  const { szenario, basis } = computeSzenario(data, szn, "2026-06-22");
  const sEnd = szenario.punkte[szenario.punkte.length - 1];
  const bEnd = basis.punkte[basis.punkte.length - 1];
  assert.ok(sEnd.liquide_cents > bEnd.liquide_cents); // Rentenzuflüsse nach Gehaltsende
});

test("Todesfall-Szenario SZN-005 rechnet ohne Wertfehler", () => {
  const data = ladeDemo();
  const szn = data.szenarien.find((s) => s.szenario_id === "SZN-005");
  const { szenario } = computeSzenario(data, szn, "2026-06-22");
  assert.ok(szenario.punkte.length > 0);
});
```

- [ ] **Step 8: Tests grün + validate**

Run: `node --test tests/m7-vorsorge-demo.test.mjs` → PASS.
Run: `npm test` → grün. `npm run validate:master` → grün (Master-`vorsorge.json` ist `[]`).

- [ ] **Step 9: Commit**

```bash
git add app/data/demo/vorsorge.json app/data/demo/zeitwerte.jsonl app/data/demo/regelzahlungen.json app/data/demo/szenarien.json app/data/master/vorsorge.json app/data-loader.mjs tests/m7-vorsorge-demo.test.mjs
git commit -m "feat(m7): komplexe vorsorge-demodaten (familie schneider) + ruhestand/todesfall-szenarien"
```

---

### Task 13: Agenten-DoD — agent-context, Skill, Next-Action

Pflicht laut Meilenstein („Betriebliches Agenten-DoD ab M6"): die deploybare App muss im Betrieb nur den App-Raum voraussetzen. M7 führt neue Datenzustände (Vorsorge), Checks und eine Annahme-Art ein — also Agenten-Doku, Skill und Next-Action-Mapping erweitern. **Keine** Verweise auf Root-Doku/ADRs/Pläne im App-Skill.

**Files:**
- Modify: `app/docs/agent-context.md` (Vorsorge-Entität, Zeitwert-felder, Beitrags-Link, Annahme-Art `vorsorge-leistung`, drei Checks)
- Create: `app/docs/skills/07-vorsorge-erfassung.md` (Onboarding aus Belegen: Standmitteilung → Rückkaufswert-Zeitwert; Renteninfo → erwartete_rente netto; Schutzversicherung → nur Beitrags-RZ; Kapitalwahlrecht; `geprueft_am` erst nach Nutzer-Review; Wechsel → `ersetzt_vorsorge_id`)
- Modify: `app/next-action.mjs` (Vorsorge-Checks ins Next-Action-Mapping/Priorität, wie bestehende Checks)
- Test: `tests/next-action.test.mjs` (vorhandene Datei: einen Vorsorge-Check-Fall ergänzen)

**Interfaces:**
- Consumes: Check-Arten `vorsorge-ungeprueft`/`vorsorge-wiedervorlage`/`vorsorge-wechsel`.
- Produces: Next-Action-Eintrag/Priorität je Vorsorge-Check.

- [ ] **Step 1: Bestehende Doku/Mechanik lesen**

`app/docs/agent-context.md`, ein vorhandener Skill unter `app/docs/skills/`, und `app/next-action.mjs` (Mapping-Struktur) öffnen. Vokabular und Format übernehmen.

- [ ] **Step 2: Next-Action-Test (rot)**

In `tests/next-action.test.mjs` einen Fall ergänzen: Daten mit einer ungeprüften Vorsorge erzeugen einen Next-Action-Eintrag mit der erwarteten Priorität/Bezeichnung. Run → FAIL.

- [ ] **Step 3: Mapping erweitern**

In `app/next-action.mjs` die drei Vorsorge-Check-Arten ins Mapping aufnehmen (Text/Priorität analog zu `marktwert-fehlt`/`bewertung-veraltet`).

- [ ] **Step 4: Test grün**

Run: `node --test tests/next-action.test.mjs` → PASS. `npm test` → grün.

- [ ] **Step 5: Agent-Doku + Skill schreiben**

`agent-context.md` um den Vorsorge-Abschnitt erweitern; `07-vorsorge-erfassung.md` neu — beide ausschließlich mit App-internen Verweisen (Schemas, Tools, Checks), keine Root-Doku/ADRs.

- [ ] **Step 6: Commit**

```bash
git add app/docs/agent-context.md app/docs/skills/07-vorsorge-erfassung.md app/next-action.mjs tests/next-action.test.mjs
git commit -m "feat(m7): agenten-DoD (agent-context, vorsorge-skill, next-action-mapping)"
```

---

## Exit-Kriterien (gegen `docs/runde2/Meilensteine_Runde2.md > M7`)

- [ ] Versicherungen und Renten in **einer** `vorsorge`-Entität mit `art`-Diskriminator und zwei Beinen (Task 1, ADR 0021).
- [ ] Laufende Beiträge mit Regelzahlungen verknüpfbar — einseitig `Regelzahlung.vorsorge_id` (Task 3).
- [ ] Spätere Leistungen als Rente, Kapitalleistung oder Ereignis modellierbar — `erwartete_rente`/`erwartete_kapitalleistung` + Annahme-Art `vorsorge-leistung`; Todesfall/Witwen-Waisen über Szenario-Annahmen (Task 9, Task 12).
- [ ] Ungeprüfte Ansprüche wirken nicht still — `geprueft_am`-Hebel: Check `vorsorge-ungeprueft` (Task 6) + Qualitäts-Deckelung auf `offen` in Szenarien (Task 9) + nicht-wegklickbare Hinweisbox (Task 11).
- [ ] Betriebliches Agenten-DoD ab M6 erfüllt (Task 13).

## Self-Review-Notiz

- **Spec-Abdeckung:** Alle zehn Grill-Entscheidungen sind abgebildet — eine Entität (T1), Kapitalwahl/drei Felder (T1/T2), zwei Verweis-Pfade + Link-Inversion (T3/T9), Nachfolge + Wechsel-Check (T1/T10), Netto-Schätzung (Demo-Bemerkungen T12) + Wiedervorlage (T6), `geprueft_am` statt Bool + keine Entitäts-`qualitaet` (T1/T6), Lebensphasen aufgeschoben (nicht enthalten — bewusst), `kapitalbildend`-Treiber (T5), Rückkaufswert-Abbau bei Auszahlung/Tod (T8/T9).
- **Typkonsistenz:** `expandVorsorgeLeistungen` erzeugt nur Primitive, die `modifizierteRegelzahlungen`/`sammleSachwertGegenbuchungen` bereits kennen (`regelzahlung-neu` mit `ab`/`bis`/`name`; `einmalzahlung` mit `datum`/`betrag`/`gegenbuchung`). Zeitwert-`feld`-Namen identisch in Schema (T2), Engine (T7–T9) und Demo (T12).
- **Offen für die Ausführung:** Render-Signatur in Task 11 und Next-Action-Mapping in Task 13 sind gegen die realen Vorbild-Dateien zu konkretisieren (Step „lesen" je Task) — bewusst nicht erfunden.
